{
  "//": "Solution-style root config. Downstream packages append themselves to `references` in sections 3/4/6/7. `include: []` avoids TS18002 (empty files) and keeps `tsc -b` a no-op with exit 0 until references are added.",
  "compilerOptions": {
    "composite": true,
    "declaration": true,
    "emitDeclarationOnly": true,
    "outDir": "./.tsbuild"
  },
  "include": [],
  "references": [
    { "path": "packages/ipc-contracts" },
    { "path": "packages/fs-datasource-engine" },
    { "path": "apps/desktop" },
    { "path": "apps/desktop/src/renderer" },
    { "path": "services/fs-monitor" }
  ]
}
