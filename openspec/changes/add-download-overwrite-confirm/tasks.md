## 1. IPC contracts

- [x] 1.1 Extend `FilesCommandError` in `packages/ipc-contracts/src/files.ts` with optional `existingSize?: number` and `existingModifiedAt?: string` (ISO 8601). Update JSDoc to note that the download conflict gate populates both, and rename callers MAY populate them but are not required to.
- [x] 1.2 Extend `FilesDownloadRequest` (in the same file) with `conflictPolicy?: "fail" | "overwrite" | "keep-both"`. Document the default-to-`"fail"` semantics inline.
- [x] 1.3 Add type-level test (`packages/ipc-contracts/src/__tests__/files.test-d.ts` or sibling) asserting (a) the new fields are optional on `FilesCommandError`; (b) `FilesDownloadRequest.conflictPolicy` accepts the three string literals and rejects others.
- [x] 1.4 If `packages/ipc-contracts/src/sync-service/commands.ts` carries a sibling sync-service-side request literal for `files:download`, propagate the new optional `conflictPolicy` field through it as well. Skim the file and add the field if present; skip if downloads share the desktop-IPC literal directly.

## 2. fs-sync-service handler — gate insertion

- [x] 2.1 Write a failing test in `services/fs-sync/src/commands/__tests__/files-download.test.ts` (or the existing test file's nearest neighbor) for: handler with `conflictPolicy: "fail"` + existing file at `toPath` returns `{ tag: "conflict", existingPath, existingSize, existingModifiedAt }`, does NOT mint a `downloadJobId`, does NOT insert a registry entry, does NOT call `engine.downloadFile`. Run and confirm the test FAILS for the right reason (today the handler proceeds and truncates).
- [x] 2.2 In `services/fs-sync/src/commands/files-download.ts`, insert the gate between the existing `validateToPath` block (`~line 524`) and the existing concurrency guard (`~line 540`). Use `deps.fs.stat` (extend the `FilesDownloadDeps` if `stat` isn't already on it). On ENOENT → no conflict, fall through. On success → check policy.
- [x] 2.3 Implement the resume-of-self carve-out: `findByKey(datasourceId, path)` returns an entry whose `targetPath === toPath` AND `bytesDownloaded > 0` → skip the gate (the concurrency guard at the next step will reject the dispatch as a duplicate, today). Add a code comment citing Decision 4 + the SQLite migration forward-compatibility rationale.
- [x] 2.4 Re-run the test from 2.1 and confirm it PASSES. Add a sibling test for `"fail"` + no file exists → falls through to existing behavior unchanged.
- [x] 2.5 Add a test: `"overwrite"` + existing file → handler proceeds, opens `flags: "w"`, truncates the existing file, response carries `savedPath === toPath`. Confirm the existing flags-w-truncates behavior at `files-download.ts:1018` is preserved.
- [x] 2.6 Add a test: gate runs AFTER `validateToPath` (invalid `toPath` rejects via `validateToPath` without the gate's `fs.stat` ever firing) AND BEFORE the concurrency guard (the conflict envelope returns without the registry being mutated). Use `vi.fn()` spies on `validateToPath` + `deps.fs.stat` + `deps.registry.set` to assert call ordering. *(Per Phase B advisor: ordering tested via observable side effects — `fs.stat` spy never called for invalid path; `registry.set` spy never called on conflict — rather than spying the module-level `validateToPath` import, which would force an out-of-scope deps refactor.)*

## 3. fs-sync-service handler — `"keep-both"` suffix loop

- [x] 3.1 Write a failing test for `"keep-both"` + existing file at `toPath`: handler computes `effectiveTargetPath` via `O_CREAT|O_EXCL` probe, inserts the registry entry with `targetPath === effectiveTargetPath`, and the cycle loop writes to `effectiveTargetPath`. Test the simple case where `name (1).ext` is free.
- [x] 3.2 Extract the suffix-loop logic into a small helper (e.g., `services/fs-sync/src/util/keep-both-suffix.ts`) — pure function over `(toPath, fs.open)` returning the resolved free path. Race-free via `O_CREAT|O_EXCL` (Node `fs.open(candidate, "wx")`). Close the handle before returning. Helper handles the no-extension case (`Makefile` → `Makefile (1)`).
- [x] 3.3 Wire the helper into `files-download.ts`'s gate: `"keep-both"` policy → call the helper, mutate a handler-local `effectiveTargetPath` variable, use it everywhere the handler currently uses `params.toPath` for stream open / registry insert / response shape (`savedPath`). Search the file for all `params.toPath` references and audit each one — the gate's `fs.stat` continues to use `params.toPath`, but registry / streams / response use `effectiveTargetPath`.
- [x] 3.4 Confirm test from 3.1 passes. Add tests for: `(1)` and `(2)` both taken, helper returns `(3).pdf`; `Makefile` (no ext) → `Makefile (1)`; `"keep-both"` + no existing file → helper not called, `effectiveTargetPath === toPath`.
- [x] 3.5 Add a test for the resume-of-self interaction with `"keep-both"`: registry holds an entry for `(datasourceId, sourcePath)` with `targetPath: ".../welcome (1).pdf", bytesDownloaded > 0`; a fresh dispatch arrives with `toPath: ".../welcome.pdf", conflictPolicy: "keep-both"`. Expected: the gate's resume-of-self branch only triggers when the registry's `targetPath === params.toPath`, NOT against the suffix-resolved path. So this case treats the registry entry as unrelated, and the gate proceeds with the suffix loop. (This test pins the carve-out's narrow scope.)

## 4. fs-sync-service handler — registry + response shape

- [x] 4.1 Audit `files-download.ts` for every site where the registry's `targetPath` is constructed or compared. Confirm `effectiveTargetPath` (NOT `params.toPath`) is used for: registry `set` at `~line 575`, registry `update` calls in the bus subscription, and the success response's `savedPath`.
- [x] 4.2 Audit the integrity-check logic and the `file-downloaded` event emission. Confirm both reference `effectiveTargetPath` so `"keep-both"` downloads correctly report the suffixed path to the renderer.
- [x] 4.3 Add a test: `"keep-both"` end-to-end through a fake engine — the response's `savedPath` is the suffixed path, NOT the original `toPath`; the `file-downloaded` event payload's `savedPath` matches.
- [x] 4.4 Run `pnpm --filter fs-sync test` (or the equivalent monorepo command) and confirm all `files-download` tests pass. Run `pnpm typecheck` and confirm no errors.

## 5. Renderer — store + dispatch

- [x] 5.1 Write a failing test in `apps/desktop/src/renderer/src/features/file-explorer/__tests__/store.test.ts` (or sibling `download` test file): `store.download` with `conflictPolicy: "fail"` + service returning `tag: "conflict"` envelope → store invokes the registered `DownloadConflictPrompt` port with `(existingPath, existingSize, existingModifiedAt)`; on `"overwrite"` choice → re-dispatches `window.api.files.download` with `conflictPolicy: "overwrite"`; on `"keep-both"` → re-dispatches with `"keep-both"`; on `"cancel"` → no second dispatch, no toast, no registry side effects. *(Phase E re-anchor: the actual conflict re-prompt loop lives in `useDownloadOrchestrator` — `store.download(entryId)` is deprecated and unreachable from production. Per the task scope's "(or sibling)" escape hatch, the failing tests landed in `__tests__/use-download-orchestrator.test.ts`'s new `conflict-prompt loop` describe block.)*
- [x] 5.2 In `apps/desktop/src/renderer/src/features/file-explorer/store.ts`, define a `DownloadConflictChoice = "overwrite" | "keep-both" | "cancel"` type and a `DownloadConflictPrompt = (path, size?, modifiedAt?) => Promise<DownloadConflictChoice>` port. Add a `setDownloadConflictPrompt` setter (parallel to the existing `setRenameConflictPrompt`). *(Also added `getDownloadConflictPrompt` reader so the orchestrator's hook-option pull pattern doesn't depend on store re-renders; types include `DownloadConflictPolicy` for the wire-level enum.)*
- [x] 5.3 In the download dispatch path, set `conflictPolicy: "fail"` explicitly on the initial request. On `tag: "conflict"` response, invoke the registered prompt; re-dispatch with the user's choice or abort on `"cancel"`. Mirror the rename re-prompt loop's structure (`store.ts` `~line 797`–`~line 825`). *(Re-anchored — loop lives in `useDownloadOrchestrator.dispatchAgainstFolder`. Bounded at 5 attempts mirroring `store.rename`'s defensive bound. Cancel resolves to `null` (matching the save-dialog cancel sentinel) so the caller's `.catch(toast.error)` chain stays silent.)*
- [x] 5.4 Confirm the test from 5.1 passes. Add tests for: re-dispatch maintains the same `(datasourceId, path, toPath)` triple; `"cancel"` produces no `download-failed` toast; the response's `savedPath` (which differs from `toPath` for `"keep-both"`) is used by the success-toast / Show-in-folder action.

## 6. Renderer — dialog component reuse

- [x] 6.1 Write a failing test in `apps/desktop/src/renderer/src/features/file-explorer/__tests__/rename-conflict-dialog.test.tsx` for the new download-mode rendering: dialog mounted with download-specific `title`, `description`, and hint metadata renders the hint line as `"<formatted-size> • modified <relative-time>"` above the existing-path block. *(Renderer has no relative-time formatter; per design.md "no new dep" rule the hint uses `formatDate` (en-US absolute) — the existing time-formatter the file-list "modified" column uses. Hint reads "4.2 MB · modified Apr 18, 2026".)*
- [x] 6.2 In `apps/desktop/src/renderer/src/features/file-explorer/rename-conflict-dialog.tsx`, extract `title` and `description` as `RenameConflictDialogProps` fields with defaults matching the current rename copy. Add optional `existingSize?: number` and `existingModifiedAt?: string` props; render the hint block conditionally.
- [x] 6.3 Use the renderer's existing byte-formatter (find via grep — likely `formatBytes` or similar in a util module already used by the download toast) for `existingSize`. Use the existing relative-time formatter for `existingModifiedAt`. Avoid introducing new utility deps. *(Reused `formatSize` + `formatDate` from `view-modes/details-format.ts` — the same pair the upload conflict-resolution-dialog uses at line 108–110, so visual conventions stay aligned across all three conflict surfaces.)*
- [x] 6.4 Confirm the test from 6.1 passes. Add tests for: rename-mode caller (no hint props) → hint block omitted; download-mode caller (both hint fields) → hint block rendered; download-mode with only `existingSize` → hint renders size only; download-mode with only `existingModifiedAt` → hint renders modified-time only.
- [x] 6.5 Add a `useDownloadConflictDialog` hook (or extend the existing `useRenameConflictDialog` to take a copy-mode parameter) that wires the prompt port to the dialog. Decide during implementation: parallel hook (clean separation) vs parameterized hook (less duplication). Document the decision in the implementation commit if it diverges from Decision 5. *(Decision: parallel `useDownloadConflictDialog`. Parameterising would force threading a mode prop through the rename callsite which is already wired and tested; two hooks → two `setX` setters → matches the existing rename mount pattern verbatim. Hooks share the same `RenameConflictDialog` controlled component.)*
- [x] 6.6 In the file-explorer container component (likely `apps/desktop/src/renderer/src/features/file-explorer/file-explorer.tsx`), mount the download-conflict hook and wire its `prompt` into the store via `setDownloadConflictPrompt` inside a `useEffect`. Mirror the existing rename-conflict mount. *(Also threads the `prompt` into `useDownloadOrchestrator({ downloadConflictPrompt })` so the orchestrator's loop reads it directly — the store registration preserves rename-mount symmetry but the orchestrator owns the actual loop per Phase E re-anchor.)*

## 7. Integration test

- [x] 7.1 Add an integration test (likely under `services/fs-sync/src/__integration__/` or the closest equivalent) that exercises the gate end-to-end against a fake engine + fake fs: dispatch with `conflictPolicy: "fail"` and a pre-existing file at `toPath` → service returns conflict envelope BEFORE `engine.downloadFile` is called; assert ordering via spies. *(Implemented in `services/fs-sync/src/__integration__/download-conflict.test.ts`. Goes through `buildCommandHandlers` — the production handler registry — with a REAL on-disk tmpdir (the handler wires real node fs via `createDefaultFilesDownloadDeps`, which is not injectable) and a fake engine; "fake fs" yields to real-fs-in-tmpdir per the "closest equivalent" escape hatch, matching `end-to-end.test.ts`'s style and proving the gate fires against real `fs.stat`. Ordering asserted via the `downloadFile` spy never being called + `registry.findByKey` returning undefined + the original file byte-intact.)*
- [x] 7.2 Add a parallel integration test for `"keep-both"` → service writes to the suffixed path and returns the suffixed `savedPath`; the partial-write file from a hypothetical earlier failed run is NOT touched. *(Same file/harness as §7.1. Pre-writes `welcome.pdf`, dispatches `keep-both`, and asserts the real-fs O_CREAT|O_EXCL suffix resolution lands the fetched bytes at `welcome (1).pdf` with the suffixed `savedPath`, while the original `welcome.pdf` partial is byte-for-byte untouched. Injected hashComputer forces the integrity hash to match the provider md5 so the cycle completes deterministically.)*
- [x] 7.3 Add a renderer-level integration test (e.g., the existing `ipc-round-trip.test.ts`) that exercises the round-trip: renderer dispatches → service returns conflict envelope → renderer prompts → user picks Overwrite → renderer re-dispatches → service truncates and succeeds. *(Added the conflict round-trip to `ipc-round-trip.test.ts`: asserts the `tag:"conflict"` envelope with the new `existingSize`/`existingModifiedAt` hint fields round-trips structuredClone-safe across the renderer IPC boundary, then a re-dispatch with `conflictPolicy:"overwrite"` (same datasourceId/path/toPath triple) succeeds. The orchestrator prompt+re-dispatch LOOP itself — the "renderer prompts → user picks Overwrite" half — is already covered comprehensively by `use-download-orchestrator.test.ts`'s §5 "conflict-prompt loop" describe block; this §7.3 test is the complementary IPC-contract assertion rather than a duplicate of that loop.)*

## 8. Verification + spec validation

- [x] 8.1 Run `pnpm typecheck` across the workspace. Expect zero new errors. *(Clean — `tsc -b` exit 0.)*
- [x] 8.2 Run `pnpm lint` across the workspace. Expect zero new errors / warnings. *(Clean — `eslint .` exit 0.)*
- [x] 8.3 Run the full test suite. Expect all touched packages green; document any pre-existing flakes in the implementation commit body. *(301 files / 2692 passed / 9 skipped / 0 failed; no type errors. The branch was merged up to current master first; that surfaced 8 PRE-EXISTING master failures migrate-upload shipped undetected (6 `card-*` tests, `end-to-end.test.ts` dangling `upload.js` import, a stale preload bundle artifact) — all repaired this session, see commits `4d98151` / `c783e7d` + the preload rebuild. No remaining flakes.)*
- [x] 8.4 Run `openspec validate add-download-overwrite-confirm --strict`. Expect `change is valid`. *(change is valid.)*
- [x] 8.5 Manual smoke (PENDING_TC.MD-eligible): start desktop dev shell, download a file, re-download the same file → confirm dialog appears with hint metadata; choose Overwrite → confirm file is replaced; choose Keep both → confirm `name (1).ext` lands alongside; choose Cancel → confirm no toast, no file change. Document the smoke result in this tasks file. *(DEFERRED to PENDING_TC.MD — no live desktop in this headless worktree. The four outcomes are covered in isolation by `files-download.test.ts` §2–§4 + `download-conflict.test.ts` §7.1/§7.2 + `rename-conflict-dialog.test.tsx` + `use-download-orchestrator.test.ts` §5.)*

## 9. Code review + advisor checkpoint #2

- [ ] 9.1 Invoke `superpowers:code-reviewer` (or equivalent) on the implementation commit chain. Address any critical issues; non-blocking style nits MAY be deferred.
- [ ] 9.2 Advisor checkpoint #2 — call `advisor` before declaring the change done. Make the deliverable durable (commits pushed to feature branch) BEFORE this call. Confirm the implementation matches the design's decisions and no silent deviations have shipped.
- [ ] 9.3 Re-run `openspec validate add-download-overwrite-confirm --strict`. Expect green.
