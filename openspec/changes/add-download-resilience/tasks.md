# Tasks: add-download-resilience

## 1. Wire-level type extensions (`@ft5/ipc-contracts`)

- [x] 1.1 Added `ExhaustedRetries: "exhausted-retries"` to the `FilesErrorTag` const-object + type alias in `packages/ipc-contracts/src/files.ts`.
- [x] 1.2 Added `DownloadRetryingPayload { downloadJobId, datasourceId, attempt, limit, waitMs, engineCause }` to `packages/ipc-contracts/src/sync-service/events.ts` plus its `EventPayloadMap` and `EVENT_NAMES` registrations. `engineCause` typed `string` (engine `DatasourceErrorTag` is itself a string union; keeping the wire field as `string` avoids cross-package coupling). Also widened `DownloadFailedPayload.tag` to include `"exhausted-retries"`.
- [x] 1.3 Updated test-d files (`__tests__/files-error-tag.test-d.ts`, `sync-service/__tests__/files-commands.test-d.ts`, `sync-service/events.test-d.ts`) plus a downstream fix in `__tests__/files.test-d.ts` for hardcoded tag-union expectations. Asserts (a) `FilesErrorTag` includes the new value, (b) `DownloadRetryingPayload` shape, (c) `DownloadFailedPayload.tag` includes `"exhausted-retries"`.
- [x] 1.4 `pnpm --filter @ft5/ipc-contracts build` clean. `pnpm exec vitest run packages/ipc-contracts/` — 44 test files / 469 tests passed, no type errors. Full-repo `pnpm typecheck` clean.

## 2. Handler retry-loop helpers (`services/fs-sync/src/commands/files-download.ts`)

- [x] 2.1 Add `isEnvironmentallyRetryable(err: unknown): boolean` near the top of the file. Returns true iff `err instanceof DatasourceError && err.tag !== "auth-expired" && err.retryable === true && (err.tag === "network-error" || err.tag === "rate-limited" || err.tag === "provider-error")`. Otherwise false.
- [x] 2.2 Add `expBackoff(attempt: number): number` returning `Math.min(1000 * 2 ** (attempt - 1), 30000)`. Pure function; unit-tested in §6.
- [x] 2.3 Add `sleepCancellable(ms: number, signal: AbortSignal): Promise<void>` — wraps `setTimeout` + a `signal.addEventListener("abort", …)` pair so an abort resolves the sleep immediately and clears the timer (`clearTimeout`). Returns void on either resolution path; never rejects.
- [x] 2.4 Add the constants `CONSECUTIVE_FAIL_LIMIT = 5` and `WALLTIME_CEILING_MS = 30 * 60 * 1000` to the file's existing constant block alongside `MAX_AUTH_RETRIES_PER_CYCLE`.
- [x] 2.5 Rename internal sentinel `RangeNotSupportedError` to `RangeNotHonoredError` (class declaration + every reference in this file). The error message string stays the same so existing wire-shape compatibility holds.

## 3. Filesystem boundary extension (`services/fs-sync/src/commands/files-download.ts`)

- [x] 3.1 Add `unlink(path: string): Promise<void>` to the `FsBoundary` interface.
- [x] 3.2 Wire the default `FsBoundary` impl in `createDefaultFilesDownloadDeps` to call `node:fs/promises.unlink(path)` for the new method.
- [x] 3.3 Add a `DELETE_ON_TERMINAL` constant — a `Set<Function>` of `RangeNotHonoredError`, `RangeMismatchError`, `IntegrityFailedError` (NOT `ByteCountMismatchError`). Confirm by code review that no other sentinel error class is mis-included.

## 4. Handler retry-loop integration (`services/fs-sync/src/commands/files-download.ts`)

- [x] 4.1 Add two new state variables to the handler closure: `consecutiveFailureCount = 0` and `walltimeStartedAt = deps.now()` (captured BEFORE the outer cycle loop begins, so it covers the whole download lifetime).
- [x] 4.2 In the inner-loop catch (the existing `try { await deps.fs.pipeline(...) } catch` block), add a SECOND branch after the existing auth-expired branch and before the final `throw err`. The new branch tests `isEnvironmentallyRetryable(err)` — when true, it: (a) increments `consecutiveFailureCount`, (b) checks `consecutiveFailureCount > CONSECUTIVE_FAIL_LIMIT` → throw a new `ExhaustedRetriesError(err.tag)`, (c) checks `deps.now() - walltimeStartedAt > WALLTIME_CEILING_MS` → throw a new `WalltimeExceededError(err.tag)`, (d) computes `wait = max(err.retryAfterMs ?? 0, expBackoff(consecutiveFailureCount))`, (e) checks `wait > (WALLTIME_CEILING_MS - elapsed)` → throw `WalltimeExceededError`, (f) emits `download-retrying { downloadJobId, datasourceId, attempt: consecutiveFailureCount, limit: CONSECUTIVE_FAIL_LIMIT, waitMs: wait, engineCause: err.tag }`, (g) `await sleepCancellable(wait, abortController.signal)`, (h) re-stat `bytesWritten` from disk, (i) continue the inner loop.
- [x] 4.3 Add the byte-progress reset rule. After the pipe drains successfully (the existing `cycleSucceeded = true` block), insert a check: if `bytesWrittenAfter > bytesWrittenBefore` (compare the just-stat'd value against the value at the start of the iteration), then `consecutiveFailureCount = 0`. Capture `bytesWrittenBefore` before issuing `engine.downloadFile`, sample `bytesWrittenAfter` after the pipe drains.
- [x] 4.4 Define two new sentinel error classes: `ExhaustedRetriesError` (carries `engineCause: DatasourceErrorTag`) and `WalltimeExceededError` (carries `engineCause: DatasourceErrorTag`). Place alongside the existing sentinels at the bottom of the file.
- [x] 4.5 In the outer terminal-catch block (the existing branch at the end of the handler), add handling for both new sentinels. Both emit `download-failed { downloadJobId, datasourceId, tag: "exhausted-retries", message: <descriptive> }`. Use `"exhausted-retries: <engineCause>"` for `ExhaustedRetriesError` and `"walltime-exceeded: <engineCause>"` for `WalltimeExceededError`. Both return `{ ok: false, error: { tag: "exhausted-retries", message, retryable: true } }`.
- [x] 4.6 In the SAME outer terminal-catch block, add the disposition policy. Before any terminal `download-failed` emission, check whether `err.constructor` is in `DELETE_ON_TERMINAL`. If yes, `await deps.fs.unlink(params.toPath).catch((unlinkErr) => { /* log warning, don't escalate */ })`. The unlink runs BEFORE the `fsSyncBus.emit("download-failed", ...)` so a renderer that subscribes to `files:download-failed` and inspects the disk sees the consistent post-disposition state.

## 5. Engine bus / event subscription (`services/fs-sync/src/commands/files-download.ts`)

- [x] 5.1 Confirm the existing engine-bus subscription does NOT need to relay `download-retrying` (it does not — the event is fs-sync-domain, not engine-domain). No new subscription branches required. [Verified: `engineBus` is injected as a handler dependency for streaming-event correlation, not as a bootstrap-level translator. `download-retrying` is emitted by the handler (files-download.ts §4 lines 745-752); the engine has no "retrying" concept. No bootstrap subscription change.]
- [x] 5.2 Confirm the existing `downloads:list-active` snapshot logic is unchanged — retry state is event-stream-only, the registry stores no retry flags. No code change. [Verified: `DownloadJobEntry` has 8 fields, none retry-related; `DownloadJobUpdate` is type-locked to `bytesDownloaded | contentLength` only. §4's `consecutiveFailureCount` + `walltimeStartedAt` live in handler closure scope. `downloads-list-active.ts` is unchanged.]

## 6. Handler unit + integration tests (`services/fs-sync/src/commands/__tests__/files-download.test.ts`)

- [x] 6.1 Unit-test `isEnvironmentallyRetryable` against the full Cartesian: every `DatasourceErrorTag` value × `retryable: true | false`. Assert the truth table: only `network-error`, `rate-limited`, `provider-error` with `retryable: true` return true; auth-expired returns false even when retryable=true; non-`DatasourceError` instances return false.
- [x] 6.2 Unit-test `expBackoff` returns `1000, 2000, 4000, 8000, 16000, 30000, 30000, 30000` for n ∈ {1..8}. Confirm cap.
- [x] 6.3 Unit-test `sleepCancellable`. Cases: (a) resolves on timer fire, (b) resolves immediately on pre-aborted signal, (c) resolves immediately when signal aborts mid-sleep, (d) clears the timer when aborted (no callback fires after).
- [x] 6.4 Integration test "Network drop mid-stream recovers transparently" per the spec scenario. Drive the engine fake to error mid-stream once with `tag: "network-error", retryable: true`; second call succeeds. Assert exactly one `download-retrying` event with `attempt: 1, waitMs: 1000, engineCause: "network-error"`. File downloads completely. Use `vi.useFakeTimers()` to fast-forward the sleep.
- [x] 6.5 Integration test "Five consecutive environmental failures exhaust the budget" per the spec scenario. Drive 5 errors in a row with no progress. Assert 5 `download-retrying` events (`attempt: 1..5`). Then assert exactly one `download-failed { tag: "exhausted-retries", message: "exhausted-retries: network-error" }`. Assert `deps.fs.unlink` was NOT called (partial kept).
- [x] 6.6 Integration test "Successful byte progress resets the consecutive counter" per the spec scenario. Sequence: error → success drains 50MB → error → success drains the rest. Assert the second `download-retrying` has `attempt: 1` (not 2). [Implementation note: handler's outer `while(true)` is single-cycle in v1 (always breaks on `bytesWritten === finalContentLength`). Actual test asserts byte-progress reset via the env-retry branch's own rule (mid-stream throw with partial progress) — exercises Decision 10 directly.]
- [x] 6.7 Integration test "Wall-time ceiling supersedes count budget" per the spec scenario. Stub `deps.now()` to advance past the ceiling between attempts. Assert `download-failed { tag: "exhausted-retries", message: "walltime-exceeded: <engineCause>" }`. Partial kept.
- [x] 6.8 Integration test "Rate-limited error honors `retryAfterMs`" per the spec scenario. Engine errors with `tag: "rate-limited", retryable: true, retryAfterMs: 5000` at a moment when expBackoff would be 1000. Assert `download-retrying.waitMs === 5000`. Sleep duration matches under fake timers.
- [x] 6.9 Integration test "Cancel during retry sleep terminates immediately" per the spec scenario. Trigger a `download-retrying` event, advance fake timers half-way through the sleep, fire abort. Assert `download-cancelled` emits, no further `download-retrying` follows, partial kept.
- [x] 6.10 Integration test "Non-retryable tag bypasses the environmental budget" per the spec scenario. Engine errors with `tag: "auth-revoked", retryable: false`. Assert immediate `download-failed { tag: "auth-revoked" }`, no `download-retrying`, no sleep, partial kept.
- [x] 6.11 Integration test "Range-not-honored deletes the partial" per the spec scenario. First cycle drains some bytes, second `engine.downloadFile` returns 200 OK without `contentRange`. Assert `deps.fs.unlink(params.toPath)` is called BEFORE the `download-failed` event fires. Verify event payload `tag: "other", message: "range not supported on this resource"`.
- [x] 6.12 Integration test "Range-mismatch deletes the partial" per the spec scenario. Mirrors 6.11 with `contentRange.start !== bytesWritten`. Assert unlink + correct event.
- [x] 6.13 Integration test "Integrity-failed deletes the partial" per the spec scenario. Drive successful pipe-drain, then provider-hash check returns mismatch. Assert unlink + `tag: "other", message: "integrity check failed"`.
- [x] 6.14 Integration test "Byte-count-mismatch keeps the partial" per the spec scenario. Drive `bytesWritten ≠ contentLength` post-pipe. Assert `download-failed { tag: "other", message: "byte count mismatch" }`. Assert `deps.fs.unlink` was NOT called.
- [x] 6.15 Integration test "`unlink` failure is non-fatal." Trigger a delete-disposition path with `deps.fs.unlink` rejecting `EACCES`. Assert the same `download-failed` event still emits. Optionally assert a warning is logged (depending on logging strategy).
- [x] 6.16 Integration test "Auth-expired co-exists with environmental retry." Sequence: network-error → recover → mid-stream auth-expired → recover via Layer 2 → success. Assert no double-budget-counting; auth-expired uses its existing slot, environmental count is independent.
- [x] 6.17 Run `pnpm --filter @ft5/sync-service test` (or the equivalent for the fs-sync test surface) and confirm all new tests pass alongside the pre-existing suite. [Result: 465 passed / 9 skipped (was 452 before §4); typecheck + lint clean.]

## 7. Renderer toast (`apps/desktop/src/renderer/src/features/file-explorer/download-job-toast.ts`)

- [x] 7.1 Add a `retrying` state to the toast's internal state machine (alongside `idle`, `downloading`, `succeeded`, `failed`, `cancelled`). Hold optional `retryContext: { attempt, limit, waitMs, engineCause }` while in the state. **Implementation note:** state machine modeled inline on `ToastEntry` via a `retrying?: boolean` flag (a full FSM is unnecessary — the marker exists only so `downloading` after `download-retrying` reverts the chrome). Retry context lives on the in-flight `toast.custom` render; no persistence needed since the renderer SHALL NOT branch on `engineCause` (Decision 9).
- [x] 7.2 Wire a handler for the new `download-retrying` IPC event. On receipt, transition state to `retrying` with the payload's context. Title remains `Downloading <filename>`; subtext switches to `Reconnecting… (<attempt>/<limit>)`. Replace the percentage indicator with a spinner glyph. Progress bar position remains unchanged from the last-rendered `downloading` event.
- [x] 7.3 On the next `downloading` event after entering `retrying` state, transition back to `downloading` state and resume normal progress rendering. No transition animation between the two.
- [x] 7.4 Wire the toast tooltip text: `Last error: <engineCause>. Waiting <waitMs>ms before retry.` Visible only when in `retrying` state.
- [x] 7.5 Confirm Cancel during `retrying` state still works — it dispatches `sync:cancel-download` exactly as it does in `downloading` state. The toast transitions to the existing cancellation appearance on the `download-cancelled` event. **Confirmation:** the toast has no Cancel UI affordance today (loading variant carries no cancel button); the renderer's existing cancel path is a `download-cancelled`-event-arrival → `toast.dismiss(toastId)` flow, which `download-job-toast.ts` preserves verbatim in `retrying` state — the `download-cancelled` handler dispatches identically regardless of whether the entry was in retrying or downloading. No code change required.
- [x] 7.6 Update the failure-toast handler to recognize `tag: "exhausted-retries"` from `download-failed`. Render the existing failed appearance with the message text shown. **Implementation:** widened the local `DownloadEvent` mirror's `tag` union to include `"exhausted-retries"`; the existing failure handler is tag-agnostic (`toast.error("Download failed: " + message)`) and threads the verbatim message through unchanged.
- [x] 7.7 [N/A — toast is the only download UI surface; no per-row download indicator consumes the download event stream] Grep confirmed: `downloading|file-downloaded|download-failed|download-cancelled` matches only `file-explorer.tsx` (orchestration / hydration only), `download-job-toast.ts`, and the toast test file. `entry-name-cell.tsx`, `pending-op-state.ts`, `pending-op-visuals.tsx`, `status-row.tsx` do not consume the download event stream.

## 8. Renderer tests (`apps/desktop/src/renderer/src/features/file-explorer/__tests__/download-job-toast.test.ts`)

- [x] 8.1 Test "Toast switches to Reconnecting sub-status on download-retrying" per the spec scenario. Render the toast at `62%`, dispatch the event, assert subtext + spinner glyph + progress bar position.
- [x] 8.2 Test "Toast snaps back to progress on next downloading event" per the spec scenario. Dispatch retry then a `downloading` with `progress: 63`; assert subtext changes back, glyph reverts.
- [x] 8.3 Test "Toast tooltip exposes diagnostic context" per the spec scenario. Hover during retrying state; assert tooltip content includes `engineCause` and `waitMs`.
- [x] 8.4 Test "Toast does NOT change appearance for auth-expired retry" per the spec scenario. Dispatch a sequence of `downloading` events with no `download-retrying` between them (auth-expired is invisible to the renderer); assert the toast never enters `retrying` state.
- [x] 8.5 Test "Cancel during retry sleep dismisses toast within 100ms" per the spec scenario. Drive a retry state, click Cancel, assert `sync:cancel-download` is dispatched immediately (no internal wait), toast transitions to cancelled when the event arrives. **Adapted scope:** the toast carries no Cancel UI button today (see §7.5 confirmation note), so the test exercises the actually-observable cancel path: drive into `retrying`, emit `download-cancelled`, assert `toast.dismiss(toastId)` is called and no error/success variants render. The "100ms" deadline is enforced upstream (`sleepCancellable` resolves immediately on abort) and is covered by §6.9 in the fs-sync layer.
- [x] 8.6 Test "Hydration to retrying toast on next download-retrying event" per the spec scenario. Initial hydration via `downloads:list-active` (downloading state), then a `download-retrying` arrives; assert the toast transitions through `downloading` → `retrying`.
- [x] 8.7 Test "Hydration to downloading toast on next downloading event" per the spec scenario. Hydration then a `downloading` event; assert no `retrying` state is entered.
- [x] 8.8 Test failure-toast handles `tag: "exhausted-retries"`. Dispatch `download-failed { tag: "exhausted-retries", message: "exhausted-retries: network-error" }`; assert failed-state appearance + message displayed verbatim.
- [x] 8.9 Run `pnpm --filter @ft5/desktop test` (or equivalent) and confirm all renderer tests pass. **Result:** `pnpm exec vitest run apps/desktop` → 155 test files, 1211 tests, all pass; type-errors clean; lint clean.

## 9. Verification

- [x] 9.1 Run the full repo test suite: `pnpm test` at the workspace root. All existing tests pass; new tests pass; no regressions. [Result: 2544 passed / 9 skipped / 0 failed across 293 test files. Initial run surfaced 1 failure on `scripts/preload-bundle.test.ts` (a state-dependent pre-existing baseline test that asserts the compiled preload artifact at `apps/desktop/dist/preload/index.js` matches the externalize-deps allowlist); after `pnpm --filter @ft5/desktop build` rebuilt the preload artifact, the test passes 2/2. The failing file is unchanged between master and feature branch (`git diff master..feature/add-download-resilience -- scripts/preload-bundle.test.ts` is empty), confirming the failure is environmental, not a regression introduced by add-download-resilience. Net new tests vs master baseline: ~+65 (§1 type-d, §2 helpers, §3 sentinels, §4 integration, §7+§8 renderer).]
- [x] 9.2 Run `pnpm typecheck` and `pnpm lint`. No new errors introduced. [Result: both exit 0.]
- [x] 9.3 Run `openspec validate add-download-resilience`. Change passes validation. [Result: `Change 'add-download-resilience' is valid`.]
- [x] 9.4 Manual smoke test (single-machine): cold-start desktop app, log in to a datasource, start a 100MB+ download, mid-flight disable wifi for ~10s, re-enable. Verify the toast shows `Reconnecting… (1/5)` during the outage and resumes when wifi returns. The downloaded file's hash matches the provider-advertised hash. [Deferred to `PENDING_TC.MD` entry "add-download-resilience — §9.4 manual wifi-drop smoke + §10.3 follow-up window" with the design.md Decision 5 visual contract and the same-id chrome-leak failure-mode watchlist (loading-template chrome leak / stale-percentage / premature dismiss). All component pieces are unit-test green (§6.4-§6.16 deterministic fs-sync integration + §8.1-§8.cov-2 renderer); only the Sonner runtime swap behavior is unobservable via mocks. Pair with any next dev-build manual-QA pass.]
- [x] 9.5 Manual smoke test (range-not-honored): difficult to trigger reliably without a fault-injection layer. Document in the PR notes that this scenario is verified via integration test only at v1; the packaged-build E2E follow-up (`wire-packaged-build-download-resilience`) covers it with mitmproxy. [Done: §6.11 / §6.12 fs-sync integration tests cover the path with deterministic fakes; proposal.md "Deferred scope" section names `wire-packaged-build-download-resilience` as the packaged-build mitmproxy follow-up. No manual reproduction required at v1.]
- [x] 9.6 Confirm `pnpm --filter @ft5/sync-service test` shows no flake on the new integration tests across 5 consecutive runs. [Result: 5/5 runs identical at 465 passed / 9 skipped / 0 failed (68 test files each). The new §6.4-§6.16 integration tests are deterministic — fake-timer / stubbed-`now()` design eliminates wall-clock flake.]

## 10. Documentation and follow-up

- [x] 10.1 Update `services/fs-sync/README.md` (if the file documents the download path) with a one-paragraph note on the three-layer retry architecture and link to `design.md`. [Done: added "Download retry architecture" section between Events and Dev mode, documenting Layer 1 / Layer 2 / Layer 3 with the budget + walltime + disposition rules and a pointer to the (post-archive) design.md path.]
- [x] 10.2 Cross-reference the two follow-up stubs (`wire-packaged-build-download-resilience`, `add-failed-download-cleanup-affordance`) in this change's `proposal.md` "Provenance" or end-of-file section so reviewers see the deferred scope. [Done: added "Deferred scope (follow-up changes)" section to proposal.md naming both follow-ups with their roles, plus a §9.4 manual-smoke pre-archive note.]
- [x] 10.3 If the smoke tests in §9.4 surface unexpected behavior (e.g., `downloading` events fire mid-retry, the toast flickers, etc.), capture in a `PENDING_TC.MD` or follow-up issue rather than block this change. [Done preemptively: the same `PENDING_TC.MD` entry created for §9.4 carries the same-id chrome-leak failure-mode watchlist (two spinners / stale percentage / premature dismiss) and the fresh-id fix recipe (`<toastId>-retrying` + `<toastId>-resumed`). When §9.4 is run, any failure-mode reproduction maps directly to the documented fix — no additional PENDING_TC follow-up entry needed. SUPERSEDED 2026-04-30 by §11 — manual smoke surfaced two real bugs (Bug A retrying→failed duplicate-toast, Bug B stuck-Reconnecting), both now in scope per Decision 12.]

## 11. Bug fixes from §9.4 manual smoke (2026-04-30)

The §9.4 wifi-drop smoke run on commit `02de096` reproduced two visible regressions:

- **Bug A**: when `download-failed` follows `download-retrying` on the same `downloadJobId`, two toasts appear simultaneously (the retrying-state custom render and the failure-state error toast both visible). Sonner's `toast.X(msg, { id })` only updates in-place when the previous toast was the same TYPE; across types (custom → error) it spawns a new toast.
- **Bug B**: after a wifi drop and reconnect, the toast stays stuck at `Reconnecting (1/5)` for >5 minutes with no progress. After `sleepCancellable(1000)` returns, the next `engine.downloadFile()` inherits a dead OS-level socket and hangs indefinitely (Windows TCP timeout >5 minutes), blocking the retry loop. v1 has no per-request timeout.

### 11.1 Decision 12 — per-attempt request timeout (handler-level)

- [x] 11.1 Add `PER_ATTEMPT_TIMEOUT_MS = 60_000` constant to `services/fs-sync/src/commands/files-download.ts` next to the existing `CONSECUTIVE_FAIL_LIMIT` / `WALLTIME_CEILING_MS`. [Done: constant added at module level with Decision 12 doc-comment explaining the 60s rationale and the synthesized-error → Layer 3 routing contract.]
- [x] 11.2 In each retry-cycle's engine-call site (the inner-loop GET issuance), wrap the engine call with a per-attempt `AbortController` whose `setTimeout(...).abort()` fires at `PER_ATTEMPT_TIMEOUT_MS`. Compose with the existing user-cancel signal via `AbortSignal.any([abortController.signal, attemptCtrl.signal])` and pass the composed signal to the engine. `clearTimeout` on every exit path (success / error / cancel). [Done: `attemptCtrl + setTimeout + AbortSignal.any` constructed per attempt; `composedSignal` flows into `DownloadOptions.signal`; `clearTimeout(attemptTimeoutHandle)` lives in a nested `try { ... } finally { ... }` inside the catch wrapper so success / error / cancel exits all clear it.]
- [x] 11.3 In the catch, distinguish user-cancel from timeout by reading `abortController.signal.aborted` FIRST. If true → existing `CancelledError` path. Otherwise, if the engine threw because the COMPOSED signal aborted, synthesize `new DatasourceError({ tag: "network-error", retryable: true, message: \`per-attempt timeout (\${PER_ATTEMPT_TIMEOUT_MS}ms)\` })` and re-throw so the existing Layer 3 env-retry catch branch picks it up. [Done: pre-stream catch checks `abortController.signal.aborted` (or `tag: "cancelled"`) FIRST → CancelledError; THEN `attemptCtrl.signal.aborted` (or AbortError) → synthesize `DatasourceError({ tag: "network-error", datasourceType: client.type, datasourceId, retryable: true, message: "per-attempt timeout (60000ms)" })`. The synthesized error AND any pre-stream-real env-retryable error route into the same Layer 3 logic (counter increment, budget/walltime guard, `download-retrying` emit, `sleepCancellable`, `continue`). Side benefit: pre-stream-real network-error retryable=true now retries identically to mid-stream, closing a latent gap. Required updating §13.20 fixture from `network-error retryable=true` → `auth-revoked retryable=false` to keep the registry-cleanup-on-terminal contract test unambiguous.]
- [x] 11.4 No engine changes (Decision 8 stays). The composed signal's behavior at the engine boundary already exists — engines accept an `AbortSignal` and surface `AbortError` on its abort. [Done: zero touches to `packages/fs-datasource-engine/`. The engine's existing `DownloadOptions.signal` accepts the composed signal verbatim; `AbortSignal.any` is a Node 20+ standard primitive (worktree runs Node 24).]

### 11.2 Bug A — retrying→failed transition fresh-id pattern

- [x] 11.5 In `apps/desktop/src/renderer/src/features/file-explorer/download-job-toast.ts` failure handler (`event.kind === "download-failed"`), when `existing.retrying === true`, dismiss the existing custom-render toast id BEFORE spawning the error toast on a fresh id (e.g. `\`\${existing.toastId}-failed\``). Mirrors the success-path post-archive fix at lines 469-472. Update `tracker.set(downloadJobId, { toastId: failedId, basename, retry, terminal: true })` to record the new id so duplicate-terminal guard still works. [Done: branch on `existing?.retrying === true` issues `toast.dismiss(existing.toastId)` then mints `failedId = \`\${existing.toastId}-failed\`` for the error toast; non-retrying path keeps same-id semantics intact. Retry-action `onClick` closure captures `failedId` so click-dismiss targets the live error toast, not the long-gone retrying-render id.]
- [x] 11.6 Update existing test `(8.cov-2)` (retrying → failed transition) to assert the dismiss-then-fresh-id ordering: `expect(toast.dismiss).toHaveBeenCalledWith("toast-A")` before `toast.error` is called, and `expect(toast.error.mock.calls[0][1].id).toBe("toast-A-failed")`. [Done: `(8.cov-2)` now asserts (a) pre-failure `toast.dismiss` not called with `"toast-A"`, (b) post-failure both `toast.dismiss("toast-A")` and `toast.error` with `id: "toast-A-failed"` fire, (c) `dismiss.mock.invocationCallOrder[0] < error.mock.invocationCallOrder[0]` (dismiss-before-spawn ordering), (d) Retry click dismisses the FAILED id. Added new `(8.cov-3)` regression-guard test pinning the loading→failed (no retrying) path keeps same-id semantics. Toast-test count: 25 → 26.]

### 11.3 Tests for §11.1 timeout path

- [x] 11.7 Add fs-sync integration test (alongside §6.4-§6.16) "Per-attempt timeout fires when engine hangs": stub `engine.downloadFile` to return a promise that NEVER resolves (the AbortSignal does the cleanup). Use `vi.useFakeTimers()`; advance `PER_ATTEMPT_TIMEOUT_MS + 1`. Assert exactly one `download-retrying { attempt: 1, engineCause: "network-error" }` event emits (the timeout-synthesized error feeds Layer 3 identically to a real network-error). Assert the second engine call is issued. [Done: new `makeHangingDownloadFile` helper returns `Promise<DownloadResult>` whose only resolution path is the supplied AbortSignal listener rejecting with AbortError. Test advances fake timers by `PER_ATTEMPT_TIMEOUT_MS + 1`, asserts retrying event with `attempt: 1, engineCause: "network-error"`, then advances 1000ms (expBackoff(1)) so the second engine call lands on the succeeding tail. Final `result.ok === true`, `hanging.callCount() === 2`.]
- [x] 11.8 Add fs-sync integration test "Per-attempt timeout counts against the budget": drive 5 hung attempts → assert `download-failed { tag: "exhausted-retries", message: "exhausted-retries: network-error" }`. Confirms the timeout-synthesized error is budget-eligible. [Done: `hangCount: 6` (attempts 1..5 emit retrying, 6th trips `> CONSECUTIVE_FAIL_LIMIT`). Pumps 400_000ms fake time (worst-case 6×60_000 + 31_000). Asserts 5 retrying events with attempts 1..5, terminal `download-failed { tag: "exhausted-retries", message: "exhausted-retries: network-error", retryable: true }`. Confirms timeout-synthesized errors are NOT special-cased in budget accounting.]
- [x] 11.9 Add fs-sync integration test "User cancel during a hung attempt fires download-cancelled, not download-retrying": stub the engine to hang, then abort the user-cancel signal halfway through `PER_ATTEMPT_TIMEOUT_MS`. Assert the cancel branch fires (NOT the timeout-synthesis branch). [Done: pumps `PER_ATTEMPT_TIMEOUT_MS / 2` (no events yet), invokes `sync:cancel-download` handler, drains microtasks. Engine fake's signal-listener rejects with AbortError when the user-cancel propagates through `AbortSignal.any`; handler's catch reads `abortController.signal.aborted === true` FIRST → CancelledError → terminal cancel. Asserts: zero `download-retrying` events, exactly one `download-cancelled`, `result.error.tag === "cancelled"`.]

### 11.4 Re-run §9.4 smoke + close-out

- [ ] 11.10 User re-runs §9.4 manual wifi-drop smoke against the post-fix build. Expected: clean swap from `Downloading … 62%` → `Reconnecting… (1/5)` → snaps back to `Downloading … 63%` on resume; no duplicate toasts; no infinite Reconnecting hang. [Iteration 1 (post commit `3c48dbb`): Reconnecting count works, Bug B fixed (no infinite hang), but Bug A duplicate toast still appears in runtime — `toast.dismiss(id)` for a `toast.custom`-rendered toast loses the race against Sonner's dismiss animation. Mocks pass because `toast.dismiss` is synchronous in tests. See §11.12 for iteration-2 fix.]
- [ ] 11.11 If §11.10 passes: `openspec archive add-download-resilience --yes` (re-archive). If it fails on a different mode, document in PENDING_TC.MD and decide.

### 11.5 Bug A iteration 2 — same-id render-mode swap (custom→custom)

The §11.10 iteration-1 smoke confirmed that `toast.dismiss(existing.toastId)` followed by `toast.error(..., { id: \`\${toastId}-failed\` })` does NOT cleanly remove the prior `toast.custom`-rendered retrying toast in real Sonner runtime — both remain visible. The mock-based tests pass because `toast.dismiss` is synchronous in tests but waits on a dismiss-animation in runtime.

Root cause: Sonner's same-id dedup works WITHIN a render mode (loading→loading, custom→custom in-place updates). Across modes (custom→error), dismiss + spawn races leave both DOM nodes briefly (or longer, depending on the prior render's lifecycle). The success-path post-archive fix (commit `2762401`) hit the same issue going loading→custom, fixed by spawning the success render on a fresh id; but for the retrying→failed case the fresh-id approach doesn't help because the prior retrying-state custom toast is what needs to disappear, and `toast.dismiss(id)` for a custom-rendered toast doesn't reliably do that synchronously.

Fix: when `existing?.retrying === true`, use `toast.custom(buildFailureRender(...), { id: existing.toastId })` — same id, same render mode (custom→custom). Sonner replaces the render in-place with no animation transition. ONE toast in the slot.

- [x] 11.12 Add a `buildFailureRender(id, basename, message, retry, dismissRequested)` helper alongside `buildSuccessRender` in `download-job-toast.ts`. Pure `React.createElement` (no JSX), red-styled wrapper, message + Retry button + close button. The visual mirrors `toast.error`'s default look (red background, error chrome, Retry action) but is hand-rolled so we control the render mode. [Done: helper signature `buildFailureRender(_id, basename, message, onRetry, onDismiss)`. Hand-rolled `React.createElement` tree with `bg-destructive/10` + `text-destructive` outer card matching the codebase's destructive-tint convention (see `confirm-delete-dialog.tsx`, `invalid-datasource.tsx`); `role="alert"` for SR urgency; headline row `✕ Download failed: <message>` + close (✕) button; conditional Retry-button row rendered only when `onRetry !== undefined` (hydrated jobs without a registered retry callback fall back to dismiss-only). Retry click invokes `onRetry()` then `onDismiss()` per §11.12 wording.]
- [x] 11.13 In the failure handler, when `existing?.retrying === true`, switch from `toast.error` to `toast.custom(buildFailureRender(...), { id: existing.toastId })`. Same id; render mode swap stays within `toast.custom`. Update `tracker.set(... { toastId: existing.toastId, ... terminal: true })` — keeps the SAME id, no `-failed` suffix needed because we're not crossing render modes. Loading→failed (no retrying state) keeps the existing `toast.error` path (loading→error type swap is a Sonner built-in transition that works correctly). [Done: handler now branches on `wasRetrying = existing?.retrying === true`. `toastId = existing?.toastId ?? generateToastId()` factored out before the branch; `tracker.set({ toastId, basename, retry, terminal: true })` records the SAME id (no `-failed` suffix). `wasRetrying` true → `toast.custom(buildFailureRender(id, basename, message, retry, () => toast.dismiss(toastId)), { id: toastId, duration: Number.POSITIVE_INFINITY })`. `wasRetrying` false → existing `toast.error(..., { id: toastId, action: { label: "Retry", onClick } })` path (loading→error built-in type swap; pinned by `(d)`, `(d2)`, `(8.cov-3)`).]
- [x] 11.14 Update `(8.cov-2)` test to assert the new contract: when prior was retrying, `toast.custom` is called (NOT `toast.error`), with `id: "toast-A"` (NOT `"toast-A-failed"`). Render the returned tree and assert it contains the failure message + a Retry button with the registered callback. Drop the dismiss-then-spawn-ordering assertion (the new path doesn't dismiss). `(8.cov-3)` regression guard for the loading→failed path stays (still uses `toast.error` on the same id). `(d)` and `(d2)` likewise unchanged. [Done: `(8.cov-2)` rewritten to assert (a) `toast.custom` called twice (retrying + failure), (b) `toast.error` NOT called for the retrying-then-failed path, (c) `toast.dismiss` NOT called pre-spawn (in-place swap replaces without dismissing), (d) failure-render `opts.id === "toast-A"` (SAME id, no suffix), (e) `opts.duration === Number.POSITIVE_INFINITY`, (f) serialized tree contains `"Download failed"` + the verbatim message + `"Retry"`. New `findButtonByText` tree-walker helper extracts the Retry button's onClick and asserts (g) `retryFn` invoked once, (h) post-click `toast.dismiss("toast-A")` fires (cleanup). `(8.cov-3)`, `(d)`, `(d2)`, `(8.8)` unchanged. Toast-test count stays 26.]
- [ ] 11.15 Re-run §9.4 manual wifi-drop smoke. Expected: ONE toast, in-place visual swap from Reconnecting → Failed render. Iteration-1's duplicate-toast symptom does NOT reproduce.

### 11.6 Bug A iteration 3 — abandon toast.custom for retrying + failure (use Sonner built-ins)

Iter-2 (commit `7550d6c`) tried same-id custom→custom render-mode swap. §11.15 smoke confirmed it doesn't work: Sonner's `Observer.create()` merges old + new toast data but keeps the old `type` field, so a toast that started life as `toast.loading` retains `type: 'loading'` even after `toast.custom` calls — the view layer renders the loading-spinner icon overlay on top of our custom JSX. Plus same-id replacement is unreliable when render mode changes.

Iter-3 abandons `toast.custom` for retrying and failure. Both transitions use Sonner built-in templates (`toast.loading` for retrying, `toast.error` for failure). Sonner's built-in type swaps (loading→loading, loading→error) are rock-solid.

A second Sonner gotcha surfaced during implementation (advisor flag): `Observer.create()` merges new data over the existing toast (`{ ...oldToast, ...newData }`) for ALL fields, not just `type`. So if a prior `toast.loading` set `description: "Last error: ..."`, a subsequent `toast.loading(msg, { id })` without a `description` field inherits the prior description (lingering diagnostic text on resumed downloading). Production must explicitly pass `description: ""` (falsy → view-layer hides the row) on (a) the resume `downloading` after retrying and (b) the `download-failed` after retrying.

- [x] 11.16 Production: remove `buildRetryingRender` + `buildFailureRender`. Add `formatRetryingMessage` + `formatRetryingDescription`. Retrying handler: `toast.loading(formatRetryingMessage(...), { id, description: formatRetryingDescription(...) })`. Failure handler: revert to single `toast.error` path (no `wasRetrying` branch); always pass `description: ""` to clear any prior retrying-state description. `downloading` handler: pass `description: ""` on every update so resume from retrying clears the diagnostic. Extend `ToastApi.loading` and `ToastApi.error` signatures to accept `description?: string`. [Done: `buildRetryingRender` + `buildFailureRender` deleted along with the `RetryingContext` interface. `formatRetryingMessage(basename, { attempt, limit })` returns `"Downloading <basename> — Reconnecting (n/limit)"`; `formatRetryingDescription({ engineCause, waitMs })` returns `"Last error: <cause>. Waiting <ms>ms before retry."`. `download-retrying` handler now `toast.loading(message, { id, description })` on the same toast id (carrying `existing?.retry` forward, setting `retrying: true`). `downloading` handler passes `description: ""` on every update — Sonner's `Observer.create()` merge spread preserves the prior description if not overridden, so the explicit empty string clears it (view layer's `toast.description ?` truthy check hides the row). `download-failed` handler collapsed to a single `toast.error` path, also passing `description: ""`. `ToastApi.loading` + `ToastApi.error` signatures gained `description?: string`. `sonnerAdapter` already forwards opts verbatim, so no wrapper change beyond a doc-comment.]
- [x] 11.17 Tests: rewrite §8.1-§8.cov-2 to expect `toast.loading` for retrying and `toast.error` for failure. Drop the iter-2 `toast.custom` assertions for retrying. Pin the description-clear contract: `(8.2)` asserts resume call passes `description: ""`; `(8.cov-2)` asserts failure call passes `description: ""`. Drop the `findButtonByText` tree-walker helper (no longer needed for `toast.error`-style retry). [Done: §8.1 asserts two `toast.loading` calls, second carries Reconnecting message + diagnostic description. §8.2 asserts resume `toast.loading({ id, description: "" })` clears prior. §8.3 asserts retrying-state description carries `engineCause` + `waitMs`. §8.4 asserts no Reconnecting text in any of the three downloading-only loading messages. §8.5 asserts cancel path is dismiss-only. §8.6/§8.7 hydration → retrying / downloading via `toast.loading`. §8.cov-1 retrying-as-first-event spawns `toast.loading` on minted id. §8.cov-2 retrying→failed: zero `toast.custom`, one `toast.error` with `id: "toast-A"`, `description: ""`, Retry callback survives, click invokes registered callback + dismisses. §8.cov-3 unchanged. §8.8 unchanged. `findButtonByText` + `serializeTree` + `ReactNodeShape` helpers removed. Toast-test count stays 26.]
- [x] 11.18 Decision 5 ratification: tooltip-on-hover → always-visible description (Sonner built-in `description` field). Add a render-contract note above the Decision 5 prose. [Done: design.md Decision 5 prefaced with a render-contract callout block (`toast.loading` for both downloading + retrying; `toast.error` for failure; no `toast.custom` for retrying). Tooltip prose replaced with always-visible-description prose explaining the iter-3 trade-off (lose hover-discoverability, gain reliable in-place chrome swap; diagnostic info still surfaced).]
- [x] 11.19 User re-runs §9.4 manual wifi-drop smoke. **Result (2026-05-01)**: PARTIAL — iter-3 fixes Bug A duplicate-toast and red-toast UI corruption (both confirmed RESOLVED by user). However the smoke surfaced FIVE NEW visible defects: (1) two `Download failed: range not supported on this resource` toasts appear simultaneously, one with Retry button and one without; (2) download progress is stuck (no percentage updates in the toast); (3) reconnect recovery still fails terminally with `Download failed: range not supported on this resource` on a native 400MB MP4 from Drive; (4) the active download toast carries no Cancel button affordance; (5) re-downloading the same filename overwrites silently (no rename-conflict prompt). User-confirmed file is a real MP4 (not a Doc-export). Iter-4 (§12) addresses (1)-(4); (5) is out of scope and spawns `add-download-overwrite-confirm`.

## 12. Bug fixes from §11.19 manual smoke (2026-05-01)

The §11.19 wifi-drop smoke against iter-3 build (commit `a6b7250`) confirmed the iter-3 toast-fix landed correctly but surfaced four new bugs that need iter-4 fixes. Decisions 3 (rewrite), 13, 14, 15 added to design.md cover the architectural choices; this section covers the implementation tasks.

### 12.1 Decision 15 — Single failure-toast emission source (Bug 1)

The orchestrator dispatch caller in `file-explorer.tsx` emits `toast.error("Download failed: ...")` from its `.then(response => ...)` branch when the response is `{ ok: false, error }`. The toaster's `download-failed` event handler ALSO emits `toast.error` for the same logical failure (with a Retry action). Result: two toasts visible, one with action, one without. Fix: remove the `.then(toast.error)` block; keep the `.catch(toast.error)` for genuine IPC-reject exceptions where no event reaches the bus.

- [x] 12.1.1 In `apps/desktop/src/renderer/src/features/file-explorer/file-explorer.tsx`, remove the `.then((response) => { if (response !== null && response.ok === false) toast.error(...) })` block from the `dispatchDownload(...)` call site (lines 567-573). Retain the `.catch((err: unknown) => { toast.error(...) })` block for IPC-reject. Add a brief inline comment explaining the toaster owns event-driven failure UX (per Decision 15) and the `.catch` is for IPC-reject where no `download-failed` event flows.
- [x] 12.1.2 Add a renderer-side test (or extend an existing `file-explorer.test.tsx` test) asserting that when `dispatchDownload` resolves to `{ ok: false, error: { tag: "other", message: "test" } }`, the test does NOT see a `toast.error` call from this code path — the toaster's separate test surface (already in `download-job-toast.test.ts`) covers the failure-toast emission. Test name: "post-job download failure does not double-toast via dispatch caller".
- [x] 12.1.3 Run `pnpm --filter @ft5/desktop test` and confirm no regressions. Existing tests that asserted the `.then` toast.error path SHALL be updated to assert its absence.

### 12.2 Decision 13 — Cancel action button on active download toast (Bug 4)

The toaster's `toast.loading(message, { id })` calls (downloading state and retrying state) currently pass no `action`. Add a Cancel action that calls `window.api.sync.cancelJob({ downloadJobId })`. Inject a `SyncActionsApi` collaborator on `DownloadToasterDeps` for testability.

- [x] 12.2.1 In `apps/desktop/src/renderer/src/features/file-explorer/download-job-toast.ts`, add a `SyncActionsApi` interface: `interface SyncActionsApi { cancelJob(req: { downloadJobId: string }): Promise<void>; }`. Add it to `DownloadToasterDeps` as `readonly syncApi?: SyncActionsApi`. Add a `resolveSyncApi(injected: SyncActionsApi | undefined): SyncActionsApi` production fallback resolving `window.api.sync.cancelJob` lazily (mirror the `resolveFilesApi` pattern). Hold `syncApi` in `createDownloadJobToaster` closure scope.
- [x] 12.2.2 In the `downloading` event handler, both spawn-new and update-existing paths: pass `action: { label: "Cancel", onClick: () => void syncApi.cancelJob({ downloadJobId }) }` to every `toast.loading(...)` call. Note: `downloadJobId` is captured in the handler's closure on the per-event callback; same id for spawn and update.
- [x] 12.2.3 In the `download-retrying` event handler, also pass `action: { label: "Cancel", onClick: () => void syncApi.cancelJob({ downloadJobId }) }` to the `toast.loading(...)` call. The action SHALL be present on the SAME id as the prior downloading-state toast — Sonner's same-id swap preserves action wiring.
- [x] 12.2.4 Extend the `ToastApi.loading` signature to accept the `action?: { label: string; onClick: () => void }` option. The Sonner adapter forwards verbatim — no wrapper change needed.
- [x] 12.2.5 In `apps/desktop/src/renderer/src/features/file-explorer/__tests__/download-job-toast.test.ts`, extend the test scaffolding so the mocked `toast.loading` accepts the new `action` option and exposes its received calls for assertion. Add a `syncApi` mock to the test deps.
- [x] 12.2.6 Add test `(12.2.6)` "downloading-state toast carries a Cancel action": dispatch a `downloading` event for `job-A`; assert the most-recent `toast.loading` call's options include `action: { label: "Cancel", onClick: <fn> }`; invoke the onClick and assert `syncApi.cancelJob` was called with `{ downloadJobId: "job-A" }`.
- [x] 12.2.7 Add test `(12.2.7)` "retrying-state toast carries a Cancel action": dispatch downloading then download-retrying; assert the second `toast.loading` call also passes a Cancel action; click invokes `cancelJob` for the same `downloadJobId`.
- [x] 12.2.8 Add test `(12.2.8)` "Cancel-action click does NOT pre-emptively dismiss the toast": click the Cancel action; assert `toast.dismiss` is NOT called from the click handler — the dismiss flows through the `download-cancelled` event handler.
- [x] 12.2.9 Add test `(12.2.9)` "failure-state toast does not render a Cancel action": dispatch downloading then download-failed; assert the `toast.error` call's options include `action: { label: "Retry", onClick }` only — no Cancel, no double-action layout.
- [x] 12.2.10 Update existing test `(8.5)` to assert that the Cancel action is present on the loading-toast's options when in retrying state. The existing `(8.5)` was scoped down to "dismiss-only" because no Cancel button existed; now it can assert the actual click path through `syncApi.cancelJob`. Remove the §11.19-era scope-down note from the test docstring.
- [x] 12.2.11 Run `pnpm --filter @ft5/desktop test`; all toaster tests pass.

### 12.3 Decision 14 — Bytes-only progress fallback (Bug 2)

Drive's `?alt=media` for some media files (notably 400MB+ MP4s) returns no `Content-Length` header. The engine surfaces `total: null`; `transformDownloadingEvent` maps to `progress: 0`; the toast sticks at "0%" forever. Fix: extend the `downloading` IPC payload with `bytesLoaded` and `bytesTotal`; renderer falls back to bytes-only formatting when total is null.

- [x] 12.3.1 In `packages/ipc-contracts/src/sync-service/events.ts`, extend `DownloadingPayload` with `readonly bytesLoaded: number;` and `readonly bytesTotal: number | null;`. Both required (no optional `?`) so existing event consumers see the new shape immediately and the runtime always carries the data. Update the corresponding `EventPayloadMap` entry.
- [x] 12.3.2 Update `packages/ipc-contracts/src/sync-service/__tests__/events.test-d.ts` to assert the new fields are present and required on `DownloadingPayload`. Run `pnpm --filter @ft5/ipc-contracts build` to verify the change compiles.
- [x] 12.3.3 In `services/fs-sync/src/commands/files-download.ts`, extend `transformDownloadingEvent` to pass `bytesLoaded: enginePayload.loaded` and `bytesTotal: enginePayload.total` into the returned payload. Same for the engine-bus subscription's emit and any other call site of `transformDownloadingEvent`.
- [x] 12.3.4 Update existing fs-sync integration tests that assert the `downloading` event shape (notably the §6.x tests in `files-download.test.ts`) to allow / assert the new `bytesLoaded` + `bytesTotal` fields. Use `expect.objectContaining(...)` where shape-strict assertions would otherwise fail.
- [x] 12.3.5 Add fs-sync integration test "downloading event surfaces bytesLoaded + bytesTotal verbatim from engine payload": engine fake reports `loaded: 5_242_880, total: 100_000_000` then `loaded: 31_457_280, total: 100_000_000`; assert two `downloading` events emit with matching `bytesLoaded` + `bytesTotal` and correct `progress: 5` then `progress: 31`.
- [x] 12.3.6 Add fs-sync integration test "downloading event surfaces bytesTotal: null when engine returns null total": engine fake reports `loaded: 5_242_880, total: null`; assert `downloading` event emits with `bytesLoaded: 5_242_880, bytesTotal: null, progress: 0`.
- [x] 12.3.7 In `apps/desktop/src/renderer/src/features/file-explorer/download-job-toast.ts`, update the `DownloadEvent.downloading` mirror type to include the new fields. Update `formatProgressMessage` to accept `(basename, progressPct, bytesLoaded, bytesTotal)` and switch behavior per Decision 14: percentage when `bytesTotal !== null && bytesTotal > 0`; `<X.X> MB` for bytes < 1 GB; `<X.XX> GB` for bytes ≥ 1 GB.
- [x] 12.3.8 Update both call sites of `formatProgressMessage` in the toaster (downloading-event handler spawn + update paths) to pass the new args. Pull `bytesLoaded` and `bytesTotal` from `event.payload`.
- [x] 12.3.9 Update `formatSeededRatio` (used by `hydrateActiveDownloads`) to also pass through `bytesDownloaded` + `contentLength` so hydration can render bytes-only when needed. Or replace with a direct call to `formatProgressMessage(basename, formatSeededRatio(...), job.bytesDownloaded, job.contentLength)`.
- [x] 12.3.10 Add renderer test `(12.3.10)` "toast shows bytes-only when bytesTotal is null": dispatch `downloading { bytesLoaded: 5_242_880, bytesTotal: null, progress: 0 }`; assert `toast.loading` called with message text `Downloading <basename> — 5.0 MB` (NOT `0%`).
- [x] 12.3.11 Add renderer test `(12.3.11)` "toast shows percentage when bytesTotal is non-null": dispatch `downloading { bytesLoaded: 167_772_160, bytesTotal: 398_458_880, progress: 42 }`; assert message text `Downloading <basename> — 42%`.
- [x] 12.3.12 Add renderer test `(12.3.12)` "toast scales to GB when bytesLoaded crosses 1 GB threshold": dispatch `downloading { bytesLoaded: 1_073_741_824, bytesTotal: null, progress: 0 }`; assert message text `Downloading <basename> — 1.00 GB`.
- [x] 12.3.13 Add renderer test `(12.3.13)` "hydration with null contentLength uses bytes-only": call `hydrateActiveDownloads([{ ..., bytesDownloaded: 52_428_800, contentLength: null }])`; assert the spawn `toast.loading` message is `Downloading <basename> — 50.0 MB`.
- [x] 12.3.14 Run `pnpm test` at workspace root. All renderer + fs-sync + ipc-contracts tests pass.

### 12.4 Decision 3 rewrite — One-shot rewrite-from-0 on range-not-honored (Bug 3)

The handler currently throws `RangeNotHonoredError` immediately when a resume request returns 200 OK without `Content-Range`. Iter-4 changes this path to consume one env-retry budget slot, unlink the partial in-flight, restart the cycle from byte 0, and gate all subsequent attempts to NOT send Range (`rangeUnsupported = true` sticky for the rest of this download).

- [x] 12.4.1 In `services/fs-sync/src/commands/files-download.ts`, add a closure-scoped `let rangeUnsupported = false;` near the existing `consecutiveFailureCount` declaration in the handler factory.
- [x] 12.4.2 Replace EVERY `rangeStart: bytesWritten` in the `DownloadOptions` construction with `rangeStart: rangeUnsupported ? 0 : bytesWritten` (or equivalent guard). Ensure both the pre-stream call site and any fallback paths use the same guard.
- [x] 12.4.3 In the post-`engine.downloadFile` validation block (currently lines 779-787), replace the existing `if (bytesWritten > 0 && result.contentRange === undefined) throw new RangeNotHonoredError();` with the rewrite-from-0 logic per Decision 3 / §12.4 spec scenario:
  - Increment `consecutiveFailureCount`
  - Check budget guards (`> CONSECUTIVE_FAIL_LIMIT` → throw `ExhaustedRetriesError("range-not-honored")`)
  - Check walltime ceiling (`elapsed > WALLTIME_CEILING_MS` → throw `WalltimeExceededError("range-not-honored")`)
  - Destroy `result.stream`
  - Emit `download-retrying { ..., attempt: consecutiveFailureCount, limit: CONSECUTIVE_FAIL_LIMIT, waitMs: 0, engineCause: "range-not-honored" }`
  - `await deps.fs.unlink(params.toPath).catch(() => {})`
  - `rangeUnsupported = true`
  - `bytesWritten = 0`
  - `continue` the inner loop
- [x] 12.4.4 Verify that the existing `RangeMismatchError` branch (line 784-786) still applies — that guard is `result.contentRange.start !== bytesWritten`, which fires only when a Content-Range IS present. After 12.4.3 changes, the guard's effective scope is unchanged.
- [x] 12.4.5 Verify that `RangeNotHonoredError` is no longer thrown anywhere in normal flow. Keep the class declaration as a defensive sentinel for the (unreachable in current flow) repeat-range-not-honored case described in design.md Decision 3.
- [x] 12.4.6 Verify that the `DELETE_ON_TERMINAL` set is updated: `RangeNotHonoredError` is REMOVED (it's no longer a terminal cause). Set keeps `RangeMismatchError, IntegrityFailedError`. The existing `unlink` block at the outer terminal catch only fires for the remaining set members.
- [x] 12.4.7 Update the renderer's `formatRetryingDescription` to omit the "Waiting Xms before retry" clause when `waitMs === 0`. New format when `waitMs === 0`: `Last error: <engineCause>. Restarting download.`. When `waitMs > 0`: existing format. Update any test that pins the exact format string.
- [x] 12.4.8 Add fs-sync integration test "range-not-honored on resume triggers in-flight rewrite-from-0 and download completes" per the spec scenario. Drive: cycle 1 drains 240MB then errors with network-error; cycle 2's engine.downloadFile returns 200 OK without contentRange; cycle 3 (after rewrite) completes cleanly. Assert: (a) one `download-retrying` event with `engineCause: "network-error"` (cycle 1→2 transition), (b) one `download-retrying` event with `engineCause: "range-not-honored", waitMs: 0` (cycle 2→3 transition), (c) `deps.fs.unlink` called once during the rewrite trigger, (d) all `engine.downloadFile` calls AFTER rewrite have `options.rangeStart === 0`, (e) final `file-downloaded` event with full byte count.
- [x] 12.4.9 Add fs-sync integration test "rewrite-from-0 consumes env-retry budget slots and exhausts at attempt 6" per the spec scenario. Drive 5 range-not-honored events in a row + 1 more. Assert 5 `download-retrying { engineCause: "range-not-honored" }` then `download-failed { tag: "exhausted-retries", message: "exhausted-retries: range-not-honored" }`. Partial preserved.
- [x] 12.4.10 Add fs-sync integration test "rangeUnsupported flag is sticky across subsequent mid-stream errors": drive: rewrite-from-0 trigger → byte progress → mid-stream network-error → env-retry sleep → next attempt's rangeStart === 0. Assert the `rangeStart` parameter on the post-rewrite mid-stream-recovery attempt.
- [x] 12.4.11 Update existing tests `(6.11)` and "Range-not-honored deletes the partial" — the OLD scenario asserted `RangeNotHonoredError` was a terminal cause. Replace with the new rewrite-from-0 contract. The pre-iter-4 test should be DELETED or repurposed; the new behavior is covered by 12.4.8-12.4.10.
- [x] 12.4.12 Run `pnpm --filter @ft5/sync-service test` and `pnpm --filter @ft5/desktop test`. All pass.

### 12.5 Verification + close-out

- [ ] 12.5.1 Run full repo test suite: `pnpm test`. All existing + new tests pass; no regressions.
- [ ] 12.5.2 `pnpm typecheck` and `pnpm lint`. Zero new errors.
- [ ] 12.5.3 `openspec validate add-download-resilience`. Green.
- [ ] 12.5.4 Commit iter-4 implementation in logical groups (e.g. one commit per §12.x section) on `feature/add-download-resilience`.
- [ ] 12.5.5 User re-runs §9.4 manual wifi-drop smoke against iter-4 build. Expected: (a) ONE failure toast on terminal failure (no duplicate); (b) percentage updates during downloading (or bytes-only count if Drive omits Content-Length); (c) on wifi-reconnect after a Drive Range-rejection, the toast briefly shows `Reconnecting (n/5)` with `Last error: range-not-honored. Restarting download.` description, then the percentage / bytes count restarts from 0 and downloads to completion; (d) Cancel button visible on the active toast and clicking it fires `sync:cancel-download` → toast dismisses.
- [ ] 12.5.6 If §12.5.5 passes: re-archive (`openspec archive add-download-resilience --yes`), commit the archive, then invoke `superpowers:finishing-a-development-branch` to close out the branch.
- [ ] 12.5.7 Spawn follow-up stub change `add-download-overwrite-confirm` for Bug 5 (silent overwrite of existing file at `toPath`) — out of scope for this change, was a pre-existing gap from `add-engine-rename-download`.

### 12.6 Decision 16 — cancel-download IPC bridge (Bug from §12.5.5 smoke)

The §12.5.5 user smoke (2026-05-02) confirmed the iter-4 wifi-recovery + retrying-toast UX work shipped correctly but surfaced two new visible defects: (a) the active download toast's Cancel button does not actually cancel the download — clicking it leaves the download running and the toast live; (b) the toast stops at bytes-only progress display even on files where percentage should be derivable. (b) is addressed in §12.7 (pending user file confirmation); this section addresses (a).

Root cause for (a): iter-4 Decision 13 wired the toaster's onClick to `syncApi.cancelJob({ downloadJobId })`. Production `resolveSyncApi` resolves to `window.api.sync.cancelJob`, which is the existing UPLOAD-job cancel (`SYNC_CHANNELS.cancelJob = "sync:cancel-job"` taking `{ jobId }`). The toaster's `{ downloadJobId }` payload had no `jobId` field, the upload-job handler ignored the unknown shape, and the actual `sync:cancel-download` service command (handler registered at `services/fs-sync/src/commands/handlers.ts:366`) never received the abort. The desktop main↔preload bridge for `sync:cancel-download` was never wired despite the wire contract documenting the command since `add-engine-rename-download` §13.15-§13.16. Iter-5 closes the bridge per design.md Decision 16. The file-explorer spec delta is corrected in lockstep (4× `cancelJob` → `cancelDownload` references plus one misattribution to `add-fs-engine-cancellation`).

- [x] 12.6.1 In `packages/ipc-contracts/src/sync-service-desktop/channels.ts`, add `cancelDownload: "sync:cancel-download"` to `SYNC_CHANNELS` between `cancelJob` and `authenticateStart`. Update `packages/ipc-contracts/src/sync-service-desktop/channels.test-d.ts` to add `cancelDownload` to the keys-tuple assertion and the value-mapping test.
- [x] 12.6.2 In `packages/ipc-contracts/src/sync-service-desktop/requests.ts`, add `SyncCancelDownloadRequest` (`readonly downloadJobId: string`) and `SyncCancelDownloadResponse` (`readonly cancelled: boolean`). Flat-result, NOT a fallible union — the service handler is idempotent (unknown jobId resolves with `{ cancelled: false }`, never errors). Re-export both types from `packages/ipc-contracts/src/sync-service-desktop/index.ts`. Add a `requests.test-d.ts` test asserting `SyncCancelDownloadRequest` is exactly `{ downloadJobId: string }` and the response is exactly `{ cancelled: boolean }`.
- [x] 12.6.3 In `apps/desktop/src/main/sync/client.ts`, add a `cancelDownload(params: CommandParams<"sync:cancel-download">, opts?: { timeoutMs?: number }): Promise<CommandResult<"sync:cancel-download">>` typed method calling `this.request("sync:cancel-download", params, opts)`. Mirrors the `cancelJob` typed-method shape (line 286). In `apps/desktop/src/main/sync/client.typed-methods.test.ts`, add a row `{ method: "cancelDownload", expectedCommand: SYNC_CHANNELS.cancelDownload, invoke: (c) => c.cancelDownload({ downloadJobId: "d-1" }) }` to the data-driven test array.
- [x] 12.6.4 Create `apps/desktop/src/main/ipc/sync/cancel-download.ts` mirroring the `cancel-job.ts` pattern: `export async function handleSyncCancelDownload(req: SyncCancelDownloadRequest, client: SyncClient = getSyncClient()): Promise<SyncCancelDownloadResponse>` calling `await client.cancelDownload({ downloadJobId: req.downloadJobId })`. No `not-cancelable` shape catch — the service handler never returns that. Create `apps/desktop/src/main/ipc/sync/__tests__/cancel-download.test.ts` mirroring the `cancel-job.test.ts` scaffold; assert (a) the handler proxies to `client.cancelDownload` with `{ downloadJobId }`, (b) returns `{ cancelled: true }` and `{ cancelled: false }` verbatim, (c) re-throws on unrelated errors.
- [x] 12.6.5 In `apps/desktop/src/main/ipc/index.ts`, register the new handler alongside the existing `SYNC_CHANNELS.cancelJob` registration: `ipcMain.handle(SYNC_CHANNELS.cancelDownload, async (_event, req: SyncCancelDownloadRequest) => handleSyncCancelDownload(req))`. Add the `SyncCancelDownloadRequest` import and the `handleSyncCancelDownload` import next to the existing `cancelJob` siblings.
- [x] 12.6.6 In `apps/desktop/src/preload/index.ts`, expose `cancelDownload: (req: SyncCancelDownloadRequest): Promise<SyncCancelDownloadResponse> => ipcRenderer.invoke(SYNC_CHANNELS.cancelDownload, req)` on the `window.api.sync` surface, slotted next to `cancelJob` (line 239). In `apps/desktop/src/preload/window-api.d.ts`, add the corresponding method signature. In `apps/desktop/src/preload/__tests__/sync-surface.test.ts`, add `cancelDownload` to the API-key tuple expectation (line 89) and a per-method test "cancelDownload(req) invokes SYNC_CHANNELS.cancelDownload with req and returns the resolved value" mirroring the existing `cancelJob` test (line 172).
- [x] 12.6.7 In `apps/desktop/src/renderer/src/features/file-explorer/download-job-toast.ts`, rename `SyncActionsApi.cancelJob` to `SyncActionsApi.cancelDownload` (parameter shape stays `{ downloadJobId: string }`). Update `resolveSyncApi` to look up `window.api.sync.cancelDownload` (not `cancelJob`) — both the type-narrowing block and the throw-on-unavailable diagnostic message. Update `buildCancelAction(downloadJobId)`'s onClick to call `void syncApi.cancelDownload({ downloadJobId })`. NO aliasing — `cancelJob` is removed from `SyncActionsApi`, not retained alongside the new name (per Decision 16 rationale).
- [x] 12.6.8 In `apps/desktop/src/renderer/src/features/file-explorer/__tests__/download-job-toast.test.ts`, rename the `syncApi.cancelJob` mock vi.fn() to `syncApi.cancelDownload`. Update tests `(12.2.6)`, `(12.2.7)`, `(12.2.8)`, `(8.5)` to assert `syncApi.cancelDownload` is the function called (not `cancelJob`); also assert the call arg shape `{ downloadJobId: "job-A" }` per existing assertions.
- [x] 12.6.9 Add renderer test `(12.6.9)` "Cancel-action click does NOT call window.api.sync.cancelJob (the upload-job cancel)": dispatch a `downloading` event for `job-A`; click the toast's Cancel action; assert the test's `syncApi.cancelJob` mock is NOT called (the entire upload-job mock surface stays untouched) and `syncApi.cancelDownload` IS called once. Regression guard against the iter-4 bug.
- [x] 12.6.10 Add renderer test `(12.6.10)` "Cancel-click then `download-cancelled` event flow dismisses toast exactly once": dispatch `downloading` for `job-A` → click Cancel → assert `toast.dismiss` NOT called yet → dispatch `download-cancelled { downloadJobId: "job-A" }` → assert `toast.dismiss(toastId)` called exactly once. Pins the click-side-no-dismiss invariant from Decision 16 ("the dismiss flows from the subsequent `download-cancelled` event").
- [x] 12.6.11 Run `pnpm --filter @ft5/ipc-contracts build && pnpm --filter @ft5/ipc-contracts test`, `pnpm --filter @ft5/desktop test`, `pnpm typecheck`, `pnpm lint`. All pass; zero new errors.
- [x] 12.6.12 Code-review the iter-5 cancel-wiring fix via `superpowers:requesting-code-review` — validate against design.md Decision 16, the spec-delta corrections, and the no-aliasing / event-driven-dismiss invariants.
- [x] 12.6.13 Commit iter-5 §12.6 changes on `feature/add-download-resilience` in two logical commits: (a) `docs(add-download-resilience): iter-5 design + spec + tasks for §12.6 cancel wiring`, (b) `fix(add-download-resilience): §12.6 — wire sync:cancel-download desktop bridge (Decision 16)`.

### 12.7 Decision 17 — pre-cycle metadata prefetch + renderer combined percent+size format (Bug 1 from §12.5.5 smoke)

User confirmed §12.5.5 (a) iter-5 smoke result (2026-05-02): the active download toast continues to display size-only `Downloading <name> — 371.3 MB` for the same Drive 400MB MP4 family that surfaced the issue in §11.19. Drive's `?alt=media` for these files does not return a `Content-Length` header, so the renderer's iter-4 bytes-only fallback engages — but the user's primary ask is percentage. Iter-4 Decision 14's fallback was either-or: percent OR bytes. The fix per Decision 17:

- **(a) Service-side metadata prefetch** — before the cycle loop, the handler issues `client.getMetadata(target)` once and uses `metadata.size` as the `bytesTotal` fallback whenever the engine response lacks a `Content-Length`. Drive native files reliably publish `size` via `files.get`. Wrapped with own `AbortController + 10s timeout` (the engine's `getMetadata` signature does not accept an `AbortSignal`). Failure non-fatal: log + `prefetchedSize: null` + continue.
- **(b) Renderer combined format** — `formatProgressMessage` switches the `bytesTotal !== null` path from `Downloading X — N%` to `Downloading X — N% (<loaded> / <total>)`. Total-driven unit scaling: when `bytesTotal >= 1 GB`, both rendered as GB; else both as MB.
- **(c) Registry contentLength preservation rule** — `onProgress` callback and engine-bus subscription handler MUST NOT overwrite `contentLength` with `null` when the engine reports `total: null`. Preserve existing `contentLength` (which may have been seeded by prefetch). Engine-reported `total` (when non-null) takes priority.
- **(d) NO hash reuse** — the post-pipe `client.getMetadata(target)` for the integrity check stays a SEPARATE round-trip. Prefetch captures size at start-of-download; post-pipe captures hash at end-of-download. Reusing the prefetched object would silently miss mid-stream overwrites by other clients.

#### Service-side implementation (TDD)

- [ ] 12.7.1 In `services/fs-sync/src/commands/files-download.ts`, add handler-scoped `let prefetchedSize: number | null = null;` declaration near the existing `consecutiveFailureCount` / `rangeUnsupported` declarations (right after the registry insertion + before the `try { while (true) ... }` cycle loop).
- [ ] 12.7.2 Add the prefetch block immediately after the registry insertion and BEFORE the cycle loop. Wrap `client.getMetadata(target)` with a `new AbortController()` and `setTimeout(() => prefetchCtrl.abort(), 10_000)`; compose with `abortController.signal` via `AbortSignal.any` so user-cancel propagates. On the catch path: if `abortController.signal.aborted`, route through the existing terminal-cancel handler (emit `download-cancelled` with `bytesDownloaded: 0, bytesTotal: null`); otherwise log a warning and continue with `prefetchedSize: null`. On success: `prefetchedSize = metadata.size ?? null`; if non-null, also seed `deps.registry.update(downloadJobId, { contentLength: prefetchedSize })`.
- [ ] 12.7.3 Extend `transformDownloadingEvent` signature to accept `prefetchedSize: number | null` as a 3rd context-bag field (`ctx: { downloadJobId, datasourceId, prefetchedSize }`). When `enginePayload.total === null && prefetchedSize !== null`, populate `bytesTotal: prefetchedSize` and recompute `progress = floor((loaded / prefetchedSize) * 100)` clamped to [0..100]. When `enginePayload.total !== null`, use the engine value verbatim (engine-reported total takes priority — Decision 17e).
- [ ] 12.7.4 Update both `transformDownloadingEvent` call sites in `files-download.ts`: the engine-bus subscription's `case "downloading"` block AND the inline `onProgress` callback (the `onProgress` callback writes the registry directly; the bus subscription is what actually emits the IPC event — only the bus call site needs `prefetchedSize` threaded). Apply Decision 17d's contentLength-preservation rule on BOTH update sites: when `total !== null`, write `total`; when `total === null`, prefer `existing?.contentLength ?? prefetchedSize ?? null` (read existing entry first via `deps.registry.get(downloadJobId)`).
- [ ] 12.7.5 Update fs-sync integration tests in `services/fs-sync/src/commands/__tests__/files-download.test.ts`: existing `transformDownloadingEvent` tests need the new `prefetchedSize` argument (pass `null` to preserve existing assertions). Existing tests that drive a fake engine without prefetch wiring need a fake `getMetadata` impl on the test client (default: returns a `FileMetadata`-shape object with `size: undefined` to keep current assertions stable, OR `size: <known total>` for the seeded path).
- [ ] 12.7.6 Add fs-sync integration test "(12.7.6) prefetch surfaces percentage on Content-Length-missing resource": fake client's `getMetadata` resolves with `metadata.size: 398_458_880`; engine emits `downloading { loaded: 167_772_160, total: null }`; assert (a) bus subscription emits `downloading { bytesTotal: 398_458_880, progress: 42 }`, (b) `registry.get(jobId).contentLength === 398_458_880`, (c) `engine.downloadFile` was called once with the GET options (the prefetch is a separate round-trip that does not call downloadFile).
- [ ] 12.7.7 Add fs-sync integration test "(12.7.7) prefetch failure falls through to bytes-only and download completes": fake `getMetadata` rejects with `DatasourceError({ tag: "network-error" })`; engine emits `downloading { loaded: 5_242_880, total: null }` then completes; assert (a) `prefetchedSize` ends up null in the wire-emit (bus emit `bytesTotal: null`), (b) the warning log line is emitted via injected `console.warn` mock, (c) terminal `file-downloaded` event fires normally, (d) registry's `contentLength` stays null throughout.
- [ ] 12.7.8 Add fs-sync integration test "(12.7.8) prefetch times out after 10s and download still completes": fake `getMetadata` returns a never-resolving Promise; assert that after the wrapper timeout (use injectable `now` + virtual time OR fake-timers) the prefetch is abandoned, `engine.downloadFile` is invoked, terminal success fires.
- [ ] 12.7.9 Add fs-sync integration test "(12.7.9) user-cancel during prefetch terminates with download-cancelled, no engine call": user cancel arrives BEFORE the prefetch resolves; assert (a) the prefetch's wrapper controller aborts, (b) exactly one `download-cancelled { reason: "user", bytesDownloaded: 0, bytesTotal: null }` event fires, (c) `engine.downloadFile` was NEVER called, (d) the cancel envelope `{ ok: false, error: { tag: "cancelled", ... } }` is returned.
- [ ] 12.7.10 Add fs-sync integration test "(12.7.10) engine-reported total takes priority over prefetched size on disagreement": prefetch resolves `size: 379_000_000`; engine emits `downloading { total: 398_458_880 }` (different value); assert bus emits `bytesTotal: 398_458_880` (engine value, NOT the stale prefetch). Pin Decision 17e priority rule.
- [ ] 12.7.11 Add fs-sync integration test "(12.7.11) Doc-export-style prefetch returns size: undefined → bytes-only fallback": fake `getMetadata` resolves with `{ ..., size: undefined }`; engine emits `downloading { total: null }`; assert bus emits `bytesTotal: null`, registry's `contentLength` stays null, `prefetchedSize` ends up null.
- [ ] 12.7.12 Add fs-sync integration test "(12.7.12) registry contentLength is NOT overwritten by null-total engine events": prefetch resolves `size: 380_000_000` → registry seeded; emit two engine `downloading { total: null, loaded: ... }` events; assert `registry.get(jobId).contentLength === 380_000_000` (still the prefetched value, NOT null) after each event. Pin Decision 17d preservation rule.
- [ ] 12.7.13 Add fs-sync integration test "(12.7.13) prefetched size is NOT reused for the post-pipe integrity hash": prefetch resolves with full `FileMetadata` carrying `providerMetadata.md5Checksum: "abc"`; engine completes successfully; assert that the post-pipe `client.getMetadata` is called AGAIN (a SECOND call, distinct from the prefetch) — the integrity hash uses the FRESH metadata from the post-pipe call, NOT the prefetched object. Pin Decision 17b separation rule.

#### Renderer-side implementation (TDD)

- [ ] 12.7.14 In `apps/desktop/src/renderer/src/features/file-explorer/download-job-toast.ts`, update `formatProgressMessage(basename, progressPct, bytesLoaded, bytesTotal)`. New behavior per Decision 17c:
  - `bytesTotal !== null && bytesTotal > 0` AND `bytesTotal >= 1_073_741_824` (1 GB): return `Downloading ${basename} — ${pct}% (${(bytesLoaded / 1_073_741_824).toFixed(2)} GB / ${(bytesTotal / 1_073_741_824).toFixed(2)} GB)`
  - `bytesTotal !== null && bytesTotal > 0` AND `bytesTotal < 1 GB`: return `Downloading ${basename} — ${pct}% (${(bytesLoaded / 1_048_576).toFixed(1)} MB / ${(bytesTotal / 1_048_576).toFixed(1)} MB)`
  - `bytesTotal === null || bytesTotal === 0`: existing bytes-only fallback (`<X.X MB>` or `<X.XX GB>` based on `bytesLoaded` magnitude). Update the docstring on `formatProgressMessage` to mention the metadata-prefetch context (the bytes-only branch is rare-path).
- [ ] 12.7.15 Update existing renderer tests in `apps/desktop/src/renderer/src/features/file-explorer/__tests__/download-job-toast.test.ts`:
  - `(12.3.11)` "toast shows percentage when bytesTotal is non-null": currently asserts message text `Downloading <basename> — 42%`; update to assert combined format `Downloading <basename> — 42% (160.0 MB / 380.0 MB)`. Update the input bytesLoaded to a value that renders cleanly in MB.
  - Any other existing assertions that pin the bare-percentage format need parallel updates.
- [ ] 12.7.16 Add renderer test "(12.7.16) GB-scale total renders both values in GB": dispatch `downloading { bytesLoaded: 773_094_113, bytesTotal: 4_294_967_296, progress: 18 }`; assert message `Downloading <basename> — 18% (0.72 GB / 4.00 GB)` (total-driven scaling — both in GB even though loaded < 1 GB).
- [ ] 12.7.17 Add renderer test "(12.7.17) MB-scale total renders both values in MB": dispatch `downloading { bytesLoaded: 167_772_160, bytesTotal: 398_458_880, progress: 42 }`; assert message `Downloading <basename> — 42% (160.0 MB / 380.0 MB)`.
- [ ] 12.7.18 Add renderer test "(12.7.18) bytes-only fallback unchanged when bytesTotal is null": dispatch `downloading { bytesLoaded: 5_242_880, bytesTotal: null, progress: 0 }`; assert message `Downloading <basename> — 5.0 MB` (existing iter-4 fallback verbatim — Decision 17c rare path).
- [ ] 12.7.19 Add renderer test "(12.7.19) hydration with non-null contentLength uses combined format": call `hydrateActiveDownloads([{ ..., bytesDownloaded: 167_772_160, contentLength: 398_458_880 }])`; assert spawn `toast.loading` message `Downloading <basename> — 42% (160.0 MB / 380.0 MB)`.

#### Verification + close-out

- [ ] 12.7.20 Run `pnpm typecheck`, `pnpm lint`, vitest on touched scope (`packages/ipc-contracts`, `services/fs-sync/src/commands`, `apps/desktop/src/renderer/src/features/file-explorer`). All pass; zero new errors.
- [ ] 12.7.21 `openspec validate add-download-resilience`. Green.
- [ ] 12.7.22 Code-review the iter-5 §12.7 fix via `superpowers:requesting-code-review` — validate against design.md Decision 17 (a-f), the spec delta refinement, and the four pivots flagged by advisor checkpoint #1 (timeout/abort, no hash reuse, contentLength preservation, total-driven unit scaling).
- [ ] 12.7.23 Commit iter-5 §12.7 changes on `feature/add-download-resilience` in two logical commits: (a) `docs(add-download-resilience): iter-5 design + spec + tasks for §12.7 metadata prefetch + combined progress format`, (b) `fix(add-download-resilience): §12.7 — pre-cycle metadata prefetch + renderer combined percent+size format (Decision 17)`. Then a third docs commit flipping the §12.7.x checkboxes.
- [ ] 12.7.24 User re-runs the same MP4-download smoke against the new build. Expected: toast renders `Downloading <basename> — N% (X.X MB / Y.Y MB)` (or GB-scaled if applicable). Cancel button still works (regression check on iter-5 §12.6).
