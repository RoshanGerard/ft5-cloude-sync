## 1. IPC contract (types-first, no implementation)

- [x] 1.1 Write a failing type-assertion test in `packages/ipc-contracts/src/__tests__/datasources.test-d.ts` asserting the shape of `DatasourceSummary` (id, displayName, providerId, status ∈ connected|syncing|paused|error, lastSyncAt: number|null, itemCount: number, usage?: { used: number; quota: number }, errorReason?: string), `ProviderDescriptor`, the frozen `providers` registry (exactly google-drive, onedrive, amazon-s3), and the request/response pairs for `list`, `add`, `remove`, `action`, `upload`. Verify the test fails because the module doesn't exist. (Confirmed RED: `Cannot find module '../datasources.js'`, 46/46 existing tests still passed.)
- [x] 1.2 Create `packages/ipc-contracts/src/datasources.ts` with exactly the types to make task 1.1 pass. No runtime code, no mocked data — types, `providers` registry as `as const`, and channel-name string literals only. Re-exported from `index.ts` for consumer ergonomics.
- [x] 1.3 Run `pnpm -w test` and confirm the new contract test passes. Run `pnpm typecheck` and confirm no regressions. (Result: 63/63 tests pass, typecheck clean, lint clean.)

## 2. Main-process handlers (mocked data, real IPC surface)

- [x] 2.1 Write a failing Vitest test in `apps/desktop/src/main/ipc/datasources/__tests__/list.test.ts` that invokes the `list` handler directly and asserts the response matches the contract, includes at least one of each `status` variant, and includes at least one provider with `capabilities.quota === false` (S3) alongside providers with `capabilities.quota === true`. (Also added a shared `store.ts` with `resetDatasourcesStore` for test isolation.)
- [x] 2.2 Implement `apps/desktop/src/main/ipc/datasources/list.ts` with a hard-coded mock fixture sufficient to make 2.1 pass. Wire it via `ipcMain.handle('datasources:list', …)` in the existing main ipc bootstrapper. (Extended `registerIpcHandlers(window)` to accept the target window so upload progress events can be routed via `webContents.send`.)
- [x] 2.3a Write and pass TDD tests + handler for `add`. Validates unknown providerId rejection; S3 created without `usage`; new datasources appear in subsequent list calls.
- [x] 2.3b Write and pass TDD tests + handler for `remove`. Missing datasourceId throws `not found`.
- [x] 2.3c Write and pass TDD tests + handler for `action` (pause/resume/sync-now). `sync-now` also advances `lastSyncAt`.
- [x] 2.3d Write and pass TDD tests + handler for `upload` with dependency-injected `showOpenDialog` / `sendProgress` / `nextTransactionId`. Cancel path throws without emitting progress; success path emits uploading → completed events scoped to the returned transactionId.
- [x] 2.4 Add `scripts/ipc-datasources-four-layer.test.ts` asserting: contract declares the expected channel set literally; every non-progress channel has a handler file; `ipc/index.ts` registers every channel via `ipcMain.handle` or `webContents.send`. Scope note: preload-layer assertion deferred to Phase 3 where preload gets wired.

## 3. Preload exposure

- [x] 3.1 Added failing Vitest cases in `apps/desktop/src/preload/__tests__/exposed-api.test.ts` (switched from Playwright to Vitest to reuse the existing preload harness that mocks `electron` at module load). Asserts `window.api.datasources.{list,add,remove,action,upload}` are functions, `onUploadProgress(id, cb)` delegates to `ipcRenderer.on` with client-side transactionId filtering, and the returned unsubscribe calls `removeListener` with the same listener ref. Confirmed RED: 8 new tests fail against the pre-existing preload with `Cannot read properties of undefined (reading 'list')`, and the updated `Object.keys(exposed).sort()` assertion flips to `["datasources","ping"]`.
- [x] 3.2 Extended `apps/desktop/src/preload/index.ts` to expose `window.api.datasources.{list,add,remove,action,upload,onUploadProgress}` via `contextBridge`. Each mutating/read method is a thin `ipcRenderer.invoke(DATASOURCES_CHANNELS.*, req)` wrapper with explicit return-type annotations from `@ft5/ipc-contracts` — no hardcoded channel strings. `onUploadProgress` filters by `transactionId` in the preload (per spec) so renderer subscribers never see unrelated events. Updated `apps/desktop/src/preload/window-api.d.ts` to mirror the runtime surface in the ambient `Window.api` type. Lifted the preload-layer skip in `scripts/ipc-datasources-four-layer.test.ts`; all three four-layer assertions now pass.

## 4. Renderer foundation (shadcn/ui + themes)

- [x] 4.1 Installed Tailwind v4 (`tailwindcss@^4.2.2`, `@tailwindcss/postcss@^4.2.2`, `postcss@^8.5.10`) into the `@ft5/renderer` workspace devDependencies (root `package.json` untouched). Created `src/styles/globals.css` with the Tailwind v4 `@import "tailwindcss";` idiom (not the v3 `@tailwind base/components/utilities` directives). Added `postcss.config.mjs` exposing `@tailwindcss/postcss` so Next.js 16 picks it up. Imported `globals.css` from `app/layout.tsx`. `pnpm --filter @ft5/renderer run build` succeeds end-to-end (Next.js static export, 3 pages).
- [x] 4.2 shadcn `init` CLI in 4.3.0 replaced `--style` / `--base-color` flags with presets; manual fallback per the brief. Wrote `components.json` (style `new-york`, baseColor `slate`, rsc `false`, css variables `true`, alias `@/*`). Installed shadcn runtime deps (`@radix-ui/react-slot`, `class-variance-authority`, `clsx`, `tailwind-merge`, `lucide-react`, plus `radix-ui` and `sonner` pulled by the primitives — all in renderer `dependencies`, none in root). Added `src/lib/utils.ts` with the standard `cn` helper. Wrote the full slate palette into `globals.css`: `:root` (light) + `.dark` (class-driven, not media-driven) define all 20 required tokens, plus a `@theme inline` bridge block mapping `--color-*` / `--radius-*` to Tailwind v4 utilities. Added `@/*` path alias to renderer `tsconfig.json`. Also removed `next-themes` from deps (pulled in by shadcn's sonner; Decision 6 rejects it) and rewrote the sonner wrapper's theme resolution to default to `"system"` (Phase 4B will wire the real store). Added `exactOptionalPropertyTypes: false` override in renderer tsconfig because shadcn-generated primitives spread `ComponentProps` onto Radix children in ways that collide with the base tsconfig's exact-optional strictness — scoped to renderer only; main / preload / packages / services keep the stricter base.
- [x] 4.3 Generated 11 shadcn primitives via `pnpm --filter @ft5/renderer exec npx shadcn@latest add button card badge dialog dropdown-menu progress tooltip input label skeleton sonner`: each landed under `src/components/ui/`. Verified via `git status` that only renderer-scoped files changed; root `package.json` diff empty.
- [x] 4.4 Wrote `src/styles/__tests__/theme.test.ts` reading `globals.css` and asserting: (a) Tailwind v4 `@import` idiom present, v3 directives absent; (b) no `@media (prefers-color-scheme)` gate anywhere; (c) both `:root` and `.dark` blocks exist; (d) every one of the 20 required tokens is declared in each; (e) dark values differ from light for every non-`--radius` token. Added a targeted ESLint override for `apps/desktop/src/renderer/src/**/__tests__/**` so tests can import `node:fs` (tests are compile-excluded and run in Node, never bundled). TDD discriminator verified: temporarily removed `--background` from `:root` → test reported `[ '--background' ]` missing; restored → green. Post-change test count 99 (was 92); `pnpm typecheck` and `pnpm lint` both exit clean.
- [x] 4.5 Wrote a failing Vitest test in `apps/desktop/src/renderer/src/features/theme/__tests__/theme-switcher.test.tsx` with 9 assertions covering: trigger a11y, menu opens on click AND keyboard (Enter), three menuitems in Light/Dark/System order, selecting Dark / Light / System effects on `document.documentElement.classList` and `localStorage['ft5.theme']`, System with matchMedia-dark and matchMedia-light branches, per-preference trigger indicator (`data-icon`), and a cross-component assertion that the shared store propagates a Dark selection to a mounted Toaster. Confirmed RED: `Failed to resolve import "../theme-switcher"` — 99/99 prior tests still passed.
- [x] 4.6 Implemented: `features/theme/theme-store.ts` (ThemePreference type, `THEME_STORAGE_KEY`, `getStoredPreference` / `setPreference` / `getEffectiveTheme` / `applyEffectiveTheme` / `subscribe` / `usePreference` hook on `useSyncExternalStore` with SSR snapshot `"system"`; module-scoped listener set for same-window notification, matchMedia `change` subscription, and cross-tab `storage` event), `features/theme/theme-switcher.tsx` (Button + DropdownMenu primitives + the new Icon adapter for sun/moon/monitor, with `data-testid="theme-indicator"` + `data-icon={name}` on the trigger icon), `features/theme/theme-script.ts` (`THEME_BOOTSTRAP_SCRIPT` — a single-IIFE ES5 string with all errors swallowed), `components/icon.tsx` (minimal IconName union — sun/moon/monitor/laptop — the ONE renderer-feature-code place that imports `lucide-react`), and wired the script into `app/layout.tsx` via `<script dangerouslySetInnerHTML={{__html: THEME_BOOTSTRAP_SCRIPT}} />` inside `<head>`. Added the `@/*` alias to `vitest.config.ts`'s `resolve.alias` so renderer tests resolve the same specifiers production code uses. Pass 4.5. Verified `pnpm --filter @ft5/renderer run build` succeeds end-to-end (3 static pages).
  - [x] 4.6a Re-wired `components/ui/sonner.tsx` from `const theme = "system"` (Phase 4A placeholder) to `const theme = usePreference()` and updated the in-file comment. Test `ThemeSwitcher › propagates theme changes to a mounted Toaster (shared store)` renders `<ThemeSwitcher /> + <Toaster />`, selects Dark, asserts the Toaster receives `theme="dark"` (via a scoped `vi.mock("sonner", …)` stub that captures the prop — Sonner's own `data-sonner-theme` only renders on the `<ol>` once a toast is queued, so prop-capture is both faithful and light-touch).
- [x] 4.7 Wrote `features/theme/__tests__/no-fouc.test.tsx` — 4 tests evaluating `THEME_BOOTSTRAP_SCRIPT` via `new Function(...)()` in a controlled jsdom harness with pre-seeded `localStorage` + stubbed `matchMedia`: `"dark"` applies `.dark`, `"light"` removes it even with OS-dark, unset + matchMedia-dark applies `.dark`, unset + matchMedia-light leaves it off. RED-discriminator proof: temporarily deleted `d.classList.add("dark")` from the dark branch of the IIFE — test 1 flipped to `expected false to be true`, other 3 stayed green. Restored; all 4 pass. Not committed with the break.
- [x] 4.8 Added `scripts/literals-ban.test.ts` — a standing guardrail that walks `apps/desktop/src/renderer/src/features/**` and `apps/desktop/src/renderer/src/components/**` (exempting `components/ui/**` and `__tests__/`) and flags hex colour literals (3/6/8 digit, outside comments), `rgb(` / `rgba(` / `hsl(` / `hsla(` / `oklch(` / `oklab(` function calls, and raw `font-size: Npx|Nrem` declarations. Comments are stripped before matching so doc-prose colour callouts don't trigger. Includes a 10-test negative-control `describe` block exercising every forbidden pattern plus line/column accuracy against synthetic strings. Passes today (no violators); intentional break verified by injecting `const _violation = "#ff0000";` into `features/theme/theme-switcher.tsx` — test failed with `theme-switcher.tsx:3:21 [hex-color-6] #ff0000`, then reverted.
- [x] 4.9 Added `scripts/lucide-react-forbidden-import.test.ts` — scans the same roots as 4.8 (exempting `components/icon.tsx`, `components/ui/**`, and `__tests__/`) and flags any `.tsx` file importing from `"lucide-react"` (named, default, or side-effect; single- or double-quoted). Regex uses `^...` under the `m` flag so it only matches import statements at line start, not occurrences in comments or unrelated packages like `@lucide/react` or `lucide-react-native`. Includes a 10-test block covering detection, near-miss rejection, and line/column accuracy. Passes today (only the Icon adapter imports `lucide-react` in the scanned non-exempt surface); intentional break verified by adding `import { Sun as _Sun } from "lucide-react";` to `features/theme/theme-switcher.tsx` — test failed with `theme-switcher.tsx:3:1`, then reverted. The `Icon` adapter itself was already built in Phase 4B as `components/icon.tsx`.

## 4b. Visual refinement infrastructure — Linear/Vercel dense-quiet flavour

_Scope note: tasks in this phase add *infrastructure* (fonts, tokens, keyframes, utilities, lint rules, and the empty-state illustration component). Rendered-component assertions that depend on `DatasourceCard` / `DatasourcesDashboard` / the empty state mounted in context are part of Phases 5 and 6; explicit cross-references point to where each is covered._

- [x] 4b.1 **Typography.** Installed `geist@^1.7.0` in `@ft5/renderer` workspace deps (justified by design.md Decision 9 — Geist Sans + Geist Mono + tabular-nums). `app/layout.tsx` imports `GeistSans` from `geist/font/sans` and `GeistMono` from `geist/font/mono` and applies both `.variable` class names to `<html>`, which Geist's Next.js adapter uses to inject `--font-geist-sans` and `--font-geist-mono` at build time (zero runtime font fetch). The Tailwind v4 `@theme inline` block in `globals.css` bridges those to `--font-sans`/`--font-mono` with `ui-sans-serif`/`ui-monospace` fallbacks (so the very first paint before the bundled woff2 decodes still has a sensible face). Test file is `src/styles/__tests__/typography.test.ts` — 7 assertions that read `layout.tsx` and `globals.css` as text (same house pattern as `theme.test.ts`). The "file-as-text" framing avoids two known traps: `next/font` is a compile-time loader that throws under raw vitest, and `GeistSans.variable` at runtime is a generated className like `__variable_abc123`, not the literal `--font-geist-sans` string. TDD RED confirmed before adding the wiring (5 of 7 tests failed with specific missing-import and missing-token messages). Renderer build passes end-to-end post-change.
- [x] 4b.2 **Density overrides (Tailwind v4 CSS-first config).** Added `--text-base: 0.875rem;` inside the `@theme inline` block in `globals.css` — Tailwind v4 uses CSS-first configuration (no `tailwind.config.ts`), so the density override goes there rather than in a separate config file. That single token is sufficient: `text-base` utility now resolves to 14px, and card `p-4`, dashboard `gap-3`, and toolbar height come from the default Tailwind spacing scale and are verified at the component level in tasks 5.1 / 5.4. Test file `src/styles/__tests__/density-config.test.ts` reads `globals.css` as text, extracts the `@theme` at-rule body (anchored to `\n@theme` to skip the doc-comment occurrence earlier in the file), and asserts `--text-base: 0.875rem` is present inside `@theme` and NOT inside `:root` / `.dark`. TDD RED confirmed before the edit (test failed with "expected '...' to match /--text-base.../").
- [x] 4b.3 **Radii ceiling lint.** Added `scripts/radii-ceiling.test.ts` (sibling of the other lint guardrails — the spec text mentioned `styles/__tests__/` but the established pattern for cross-cutting lints is `scripts/`). Scans `.tsx` files under `apps/desktop/src/renderer/src/features/**` and non-`ui/` `components/**`, exempting `components/ui/**` and `__tests__/`. Flags `rounded-lg`, `rounded-xl`, `rounded-2xl`, `rounded-3xl`, `rounded-full` — with a single exception: files whose basename contains `dialog` may use `rounded-lg` only (not the larger sizes). Class-match regex is whitespace/quote/brace-bounded so `rounded-lgish` and similar near-misses don't trigger. Includes a 10-test negative-control block covering each forbidden class, the dialog exception, near-miss rejection, and line/column accuracy. Passes today (no violators). Intentional-break verification: added `rounded-xl` to `features/theme/theme-switcher.tsx` → test failed with `theme-switcher.tsx:61:31 rounded-xl`; reverted. Not committed with the break.
- [x] 4b.4 **Motion keyframes + Tailwind animation tokens.** Added `@keyframes skeleton-shimmer` (horizontal sweep via `background-position` from `-200%` to `200%`, 1.5s linear infinite) and `@keyframes sync-pulse` (opacity `1 ↔ 0.5`, 1.2s ease-in-out infinite) to `globals.css`, both nested inside `@media (prefers-reduced-motion: no-preference)`. Exposed via the Tailwind v4 `@theme inline` block as `--animate-skeleton-shimmer: skeleton-shimmer 1.5s linear infinite;` and `--animate-sync-pulse: sync-pulse 1.2s ease-in-out infinite;` — since Phase 4A went CSS-first (no `tailwind.config.ts`), the animation tokens live alongside the colour/radius/typography tokens already there. Test file `styles/__tests__/motion-keyframes.test.ts` is 7 assertions over globals.css read as text: reduced-motion media block extraction, both `@keyframes` names nested inside it, neither keyframes declared at top level, both `--animate-*` variables present in `@theme inline`, both variables use the exact design.md Decision 10 timing string. TDD RED confirmed before the edit (7/7 failed with specific missing-block messages); all green after.
- [x] 4b.5 **Motion-budget lint.** Added `scripts/motion-budget.test.ts` — sibling of the other lint guardrails in `scripts/` (the spec text mentioned `styles/__tests__/` but the established pattern for cross-cutting lints is the `scripts/` root). Scans `.tsx` under `features/**` and non-`ui/` `components/**`, plus every `.css` under `styles/**`. Allowed: `animate-skeleton-shimmer`, `animate-sync-pulse`, `transition-colors`, any `data-[state=*]:animate-*` Radix variant, `motion-safe:` / `motion-reduce:` wrappers. CSS allowed: `animation: <whitelisted-name>` shorthand, `transition-property` restricted to `color | border-color | background-color`, `--animate-*` custom-property declarations, `@keyframes` bodies. Everything else fails. 20 tests: 10 positive + 10 negative cases plus repo-wide clean assertion. Intentional-break verification: added `animate-bounce` to `features/theme/theme-switcher.tsx` (className on the Icon) — test failed with `theme-switcher.tsx:61:31  [forbidden-animate-class]  animate-bounce`; reverted. Not committed with the break.
- [x] 4b.6 **Glass-on-overlay customisation.** Edited `components/ui/dialog.tsx` (overlay: `backdrop-blur-md` + `bg-black/40`; content: `backdrop-blur-md` + `bg-background/80` light + `dark:bg-background/70`), `components/ui/dropdown-menu.tsx` (both `DropdownMenuContent` and `DropdownMenuSubContent`: `backdrop-blur-md` + `bg-popover/80` + `dark:bg-popover/70`), and `components/ui/tooltip.tsx` (`backdrop-blur-sm` + `bg-foreground/90` + `shadow-md`). Test file `components/ui/__tests__/glass-overlay.test.tsx` is 6 assertions: each of the four overlay surfaces (Dialog overlay, Dialog content, DropdownMenu content, Tooltip content) carries the expected `backdrop-blur-*` class; DropdownMenu and Dialog content also carry the light+dark semi-transparent bg variants; a rendered shadcn `Card` does NOT carry any `backdrop-blur-*` class (overlays-only scope). Tests polyfill `ResizeObserver` for jsdom since Radix Tooltip depends on it at mount. GREEN after primitive edits.
- [x] 4b.7 **Reduced-motion gate — primitive-level verification.** Test file `components/ui/__tests__/reduced-motion.test.tsx`. Key divergence from the task prose: jsdom does not parse or apply external Tailwind stylesheets, so `getComputedStyle(el).animationDuration` returns `""` regardless of whether `motion-safe:` is present — that route passes tautologically. The structural assertion here reads the rendered element's `className` string and asserts every motion-producing class token (`animate-*`, `fade-*`, `zoom-*`, `slide-*`) carries a `motion-safe:` variant. 6 tests: three primitive assertions (Dialog overlay, Dialog content, DropdownMenu content) plus three regression controls (extractVariants correctness, ungated-class failure, gated-class success). GREEN path was adding `motion-safe:` to every `data-[state=*]:animate-*`, `fade-*`, `zoom-*`, `slide-in-from-*` utility on the three primitives. Feature-level verification (pulse and shimmer actually stopping in the rendered dashboard) deferred to task 5.4.
- [x] 4b.8 **Empty-state illustration component.** Created `features/datasources/illustrations/empty-datasources.tsx` exporting `EmptyDatasourcesIllustration` — a 240×160 inline SVG of a dashed cloud above three stacked datasource tiles connected by dotted lines (monochrome + single accent, per design.md Decision 12). Colour strategy: `stroke="currentColor"` for primary strokes so the illustration inherits theme text colour; `fill="var(--primary)"` for the accent tiles so they re-colour with the theme. **Important deviation from the task prose:** the spec mentioned `hsl(var(--primary))` but the literals-ban guardrail (from task 4.8) flags `hsl(` as a forbidden colour-function in feature code, and `--primary` already resolves to a complete oklch() colour — wrapping in `hsl()` would both break the guardrail and be redundant. The illustration test asserts `var(--primary)` directly on the rendered child instead. `role="img"`, `aria-label`, and nested `<title>` all deliver the accessible name "No cloud datasources connected yet" (overridable via the `title` prop). Test file `features/datasources/__tests__/illustration.test.tsx` is 5 assertions: single `<svg>` with `data-illustration="empty-datasources"`, numeric viewBox at 240×160, `role="img"` + datasource-mentioning accessible name, at least one `currentColor` child, at least one `var(--primary)` child. RED confirmed before implementation; GREEN after. Usage in the empty-state panel is task 5.1.

## 5. Renderer — datasources dashboard + store

- [x] 5.1 Wrote `apps/desktop/src/renderer/src/features/datasources/__tests__/dashboard.test.tsx` with four scenarios: loading state (never-resolving list() → at least one `[class*='animate-skeleton-shimmer']` element visible, no `[data-testid='datasource-card']` nodes), empty state (resolved `{datasources: []}` → `data-illustration='empty-datasources'` present, heading matches `/no cloud datasources/i`, primary "Add datasource" button inside the empty panel is focusable), populated state (non-empty fixture → one `[data-testid='datasource-card']` per summary in response order via display-name heading comparison, grid container carries `gap-3`), failed state (rejected list() → error message visible + "Retry" button that re-invokes `list()` successfully). RED confirmed: `Failed to resolve import "../store"`, 199 prior tests untouched. Mocks `window.api.datasources.{list,add,remove,action,upload,onUploadProgress}` plus `ping` via `vi.fn()` in `beforeEach` so each test controls resolve/reject timing; `@testing-library/react` `within()` scopes the empty CTA query to avoid colliding with the toolbar's Add datasource trigger.
- [x] 5.2 Implemented `features/datasources/store.tsx` — `DatasourcesProvider` on `useReducer` with discriminated-union `DatasourcesState` (`loading | empty | populated | error`), typed `DatasourceActions` ({`refresh, add, remove, action, upload`}), and `useDatasources() / useDatasourceActions()` hooks that throw if used outside a provider. Also created `features/datasources/dashboard.tsx` exporting `DatasourcesDashboard` (main + `DatasourcesToolbar` with `data-testid='add-datasource-trigger'` + `<ThemeSwitcher />`) and `DashboardStates` (loading → three `animate-skeleton-shimmer` skeleton cards; empty → `EmptyDatasourcesIllustration` + heading + CTA; error → `role="alert"` panel with Retry; populated → `gap-3` grid of `DatasourceCard`s). `store.tsx` uses `useReducer` dispatch actions `load/start`, `load/success` (flips to `empty` when array is length 0), `load/failure`, `datasource/added|removed|updated` — mutations call `window.api.datasources.*`, the store only reconciles on successful response, errors bubble through the returned promise. Added placeholder `features/datasources/card.tsx` exporting a minimum-viable `DatasourceCard` (card root with `data-testid='datasource-card'` + `data-datasource-id` + `<h3>` for the display name) so task 5.1's populated-state assertion has one queryable heading per summary; full card implementation comes with 5.4. GREEN: 4/4 new tests pass; suite total 203/203 (from 199); typecheck clean.
- [x] 5.3 Wrote `features/datasources/__tests__/card.test.tsx` — 12 assertions across four describe blocks: spec scenarios (all required fields render, `lastSyncAt: null` renders "Never synced", S3 omits usage bar AND no `role='progressbar'`, quick-action menu exposes Sync now / Pause / Upload from local… / Settings / Remove in order when status is connected, label flips to "Resume" when status is paused, error status exposes the reason in DOM text and the badge's `aria-label="Status: error — <reason>"`); visual refinement (`p-4` on root, `tabular-nums` on last-sync + item-count + usage containers, `motion-safe:animate-sync-pulse` on the syncing dot, NO `backdrop-blur-*` anywhere on the card subtree); provider-registry integration (icon lookup per providerId via `providers[id].icon` → Icon adapter → `data-icon` for google-drive/onedrive/amazon-s3, no hardcoded names); feature-level reduced-motion (matchMedia stubbed to return `matches: true` for `(prefers-reduced-motion: reduce)`, structural assertion that the dot carries ONLY the `motion-safe:animate-sync-pulse` token, not the bare `animate-sync-pulse`). Notes: tests wrap each card render in `<DatasourcesProvider>` with a `window.api` stub so `useDatasourceActions()` resolves; the provider's `list()` is a never-resolving promise so the provider state never disturbs the isolated card render. SVG `className` on the syncing dot is an `SVGAnimatedString`, not a plain string, so assertions read `getAttribute("class")` directly. RED confirmed before implementation: `Failed to resolve import "../card"` + nine card-scenario failures after a placeholder existed; 199 prior tests untouched.
- [x] 5.4 Implemented `features/datasources/card.tsx` — `DatasourceCard({summary})` composes shadcn `Card` (with explicit `gap-3 p-4` overriding the default `gap-6 py-6`), the `Icon` adapter (extended to include `cloud`, `database`, `hard-drive` so the provider registry's `"cloud" | "database"` strings resolve), a header row with truncated display name + provider display name, shadcn `Badge` with `aria-label="Status: <status>"` (error badge gets `"Status: error — <reason>"`), an inline `tabular-nums` meta row for last-sync + item count, shadcn `Progress` usage bar (only when `providers[id].capabilities.quota === true` — the S3 branch omits it), the error-reason paragraph when `status === "error"`, and `DropdownMenu` quick-actions. Syncing dot renders as an SVG `<circle>` with `motion-safe:animate-sync-pulse` rather than a fully-rounded `<span>` — the radii-ceiling guardrail caps feature code at `rounded-md`, so a CSS-circle approach isn't available. Inline formatting helpers (`formatItemCount` via `Intl.NumberFormat`, `formatLastSync` emitting "Just now" / "Nm ago" / "Nh ago" / "Nd ago" / "Nmo ago" / "Ny ago" / "Never synced", `formatBytes` with binary units rendered in decimal-style labels) keep the card dependency-free. Also extended `apps/desktop/src/renderer/src/types/window-api.d.ts` so the renderer typechecker sees the full `window.api.datasources.*` surface (previously the ambient only declared `ping`; renderer and preload each carry their own `window-api.d.ts`). GREEN: 12/12 card tests pass + 4/4 dashboard tests pass; suite total 215/215 (from 203); typecheck and lint clean.
- [x] 5.5 Replaced `app/page.tsx` with a thin composer that wraps `DatasourcesDashboard` in `DatasourcesProvider`. Relocated the ping probe to `app/diagnostics/page.tsx` and moved the existing test file from `app/__tests__/ping-page.test.tsx` to `app/diagnostics/__tests__/page.test.tsx`, updating the import from `PingPage` to `DiagnosticsPage` (component renamed). Updated `apps/desktop/e2e/ping.spec.ts` so the Playwright test navigates to `app://./diagnostics` via `window.goto()` after `domcontentloaded`; inline comment notes the e2e cannot be executed in the dev sandbox (no packaged build available) and that a real run happens against `pnpm --filter @ft5/desktop package:{os}` output. Toolbar already ships from task 5.2 — `DatasourcesToolbar` component with `data-testid="add-datasource-trigger"` button (local `useState` toggle for now; the actual dialog wires up in Phase 6) plus the Phase-4B `<ThemeSwitcher />`. One cross-cutting fix was required to unblock the renderer build: `@ft5/ipc-contracts/package.json` previously pointed `main` at `src/index.ts`, and the package's internal `export … from "./datasources.js"` re-export resolved fine under `tsc` + NodeNext but failed under Next 16's Turbopack bundler (and webpack fallback) because the bundler treated the `.js` specifier literally and couldn't find a sibling `.js` next to the TS source. Pointed `main` at `dist/index.js` with a new `exports` map that keeps `types` at `src/index.ts` — the existing `tsc -b` invocation under `pnpm typecheck` already populates `dist/` so this is a type-preserving hybrid. All four verification commands pass: `pnpm -w test` (215/215), `pnpm typecheck` (clean), `pnpm lint` (clean), `pnpm --filter @ft5/renderer run build` (2 static routes: `/` dashboard + `/diagnostics` ping).

## 6. Renderer — add-datasource dialog

- [x] 6.1 Wrote `features/datasources/__tests__/add-dialog.test.tsx` — 9 tests (one `describe.each` parameterized over all three providers flattens to 3 subtests, plus 6 standalone) covering: dialog opens from the toolbar trigger (role=dialog + name=/add datasource/i); step 1 lists exactly the providers from the registry via `Object.keys(providers)` so a 4th entry would automatically surface; each provider advances to step 2 with the credential form dispatched from `credentialsSchema` (OAuth → Connect button; AWS → access-key/secret/bucket labelled inputs); OAuth submit calls `add({providerId, credentials})`, closes dialog, returns focus to the trigger, appends a new card; AWS submit sends typed credentials; Back button returns to step 1; Escape closes without calling `add()`. Mocks `window.api.datasources.*` via `vi.fn()` in `beforeEach` with ResizeObserver polyfill for Radix. Confirmed RED (12/12 new cases failed before implementation).
- [x] 6.2 Wrote a separate `features/datasources/__tests__/add-dialog-extensibility.test.tsx` with a file-scoped `vi.mock("@ft5/ipc-contracts", …)` using `vi.importActual` to inject a 4th fixture provider `dropbox` with `credentialsSchema: "custom"` into the `providers` registry. Sibling tests are unaffected because the mock is file-scoped. Asserts (a) picker shows exactly 4 options; (b) selecting `dropbox` mounts `[data-testid="credential-form-custom"]` — the `CustomForm` placeholder; (c) the dialog + picker source files contain NO `providerId === "..."` branches via an fs.readFileSync scan with a small `stripComments` helper that preserves string literals (mirrors the literals-ban pattern). This codifies the extensibility scenario from the spec.
- [x] 6.3 Implemented `features/datasources/add-dialog.tsx` (shadcn Dialog + two-step state machine `pick|credentials` + exhaustive-switch dispatch on `descriptor.credentialsSchema` — `assertNever`-style default so a new schema value fails typecheck at the one-edit site), `features/datasources/provider-picker.tsx` (iterates `Object.values(providers)` and renders one `<button data-testid="provider-option-<id>">` per entry with the Icon adapter for the descriptor icon + a capability-derived summary line), and `features/datasources/credential-forms/{oauth-form,aws-access-key-form,custom-form}.tsx` (OAuth: `delayMs` prop defaulting to 800 — tests pass `0` to avoid `vi.useFakeTimers()` plumbing; AWS: four labelled shadcn Inputs + required-field validation; Custom: JSON textarea with try/catch parse + inline parse-error). Wired `<AddDatasourceDialog open onOpenChange returnFocusTo />` into `DatasourcesDashboard`, lifting dialog-open state + tracking the opener `HTMLElement` via `openerRef` so `onCloseAutoFocus` can `preventDefault()` and explicitly focus the trigger on close (Radix's default restoration was unreliable under jsdom). Error handling: inline `role="alert"` text in the dialog — the renderer does not currently mount a `<Toaster />`, so a toast would have required new infrastructure. GREEN: 230/230 tests (13 new — 9 dialog + 3 extensibility + 1 stripComments self-test), typecheck clean, lint clean, `pnpm --filter @ft5/renderer run build` clean. All guardrails still pass (radii-ceiling allows `rounded-lg` inside `*dialog*.tsx`; the picker and credential forms stay at `rounded-md`).

## 7. Diagnostics route + app-shell modification

- [ ] 7.1 Write a failing Playwright test update in `apps/desktop/e2e/ping.spec.ts`: the e2e should navigate to `app://./diagnostics` (not `/`) and assert the ping result is visible. Until the route exists, this fails.
- [ ] 7.2 Implement `apps/desktop/src/renderer/src/app/diagnostics/page.tsx` containing the existing ping probe. Delete the ping code from `app/page.tsx` if it wasn't already displaced by task 5.5. Pass 7.1.
- [ ] 7.3 Add the developer keyboard shortcut `Ctrl/Cmd + Shift + D` in the renderer shell (global listener at the layout level) that navigates to `/diagnostics`. Add a Vitest test asserting the keydown handler triggers the navigation.
- [ ] 7.4 Update the app-shell spec delta's modified requirement — this is already captured in `specs/datasources-ui/spec.md` MODIFIED section; verify `pnpm -w test` still passes against `openspec/specs/app-shell/spec.md` after archive (tracked separately in task 10).

## 8. Design docs

- [ ] 8.1 Write `docs/design/datasources-ui.md` with: ASCII wireframes for dashboard (all three states), card anatomy (annotated), add-dialog (both steps, each credential form), and the developer-only diagnostics view. Document the card state machine (statuses and transitions driven by IPC events) and list WCAG 2.2 AA requirements the primitives satisfy.
- [ ] 8.2 Include the `ProviderDescriptor` shape and registry-extension walkthrough ("to add Dropbox: X, Y, Z") with a runnable code example.

## 9. Accessibility + lint enforcement

- [ ] 9.1 Write a Vitest test that fails if any file under `apps/desktop/src/renderer/src/components/ui/` contains a regex-matched hex colour, `px` literal (outside allowed values), or `font-size:` literal outside the token module. Pass it by ensuring primitives are clean.
- [ ] 9.2 Add ESLint rules (renderer-scoped) rejecting imports of `googleapis`, `@microsoft/microsoft-graph-client`, `@aws-sdk/client-s3`, and any other provider SDK name. Add the dedicated CI grep step. Write a test that fixes a temporary offender file and then removes it; confirm the lint + grep both catch it.
- [ ] 9.3 Add a Vitest test that runs axe (or a lightweight a11y assertion script) against the rendered dashboard in all three states and the add-dialog; fail on any violation of roles, names, or contrast. (If axe adds weight we don't want, substitute a minimal role/name assertion pass — document the decision inline.)

## 10. Verification before archive

- [ ] 10.1 Run `pnpm -w test` — all tests (existing + new) pass. Record pre-change and post-change counts in the PR description.
- [ ] 10.2 Run `pnpm typecheck` and `pnpm lint` — zero errors. Record in the PR description.
- [ ] 10.3 Run `pnpm --filter @ft5/desktop run dev` and manually exercise: dashboard loading state is visible briefly; empty state appears with the custom SVG illustration; add-datasource dialog opens with backdrop-blur overlay; each of the three providers completes a mocked add; cards render with the documented fields in Geist Sans, numerics in tabular figures that don't wobble as values update; S3 card omits the usage bar; quick-actions menu opens as a blurred surface and closes with keyboard; Upload action opens the native file-picker; `Ctrl/Cmd + Shift + D` navigates to `/diagnostics`; ping result appears; the theme switcher cycles Light → Dark → System and the selection persists across a window reload with no flash of wrong theme on cold start; the "syncing" status dot pulses; setting the OS "reduce motion" preference kills the pulse and the skeleton shimmer. Record the walkthrough outcome in the PR description.
- [ ] 10.4 Run `pnpm --filter @ft5/desktop package:win` and confirm the produced `apps/desktop/release/win-unpacked/FT5 Cloude Sync.exe` launches the dashboard as its home view (not the old ping page). Record the artifact size and launch time in the PR description.
- [ ] 10.5 Confirm no edits to `openspec/specs/` outside of what the spec delta at `openspec/changes/ui-ux-design/specs/datasources-ui/spec.md` declares (verified by `git diff --stat openspec/specs/` being empty on the branch).
